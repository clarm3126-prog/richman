# 작업일지

세션 셋이 같은 저장소를 만진다. 커밋 메시지는 **무엇을** 바꿨는지는 남기지만
**왜** 그랬는지, 그래서 **다른 세션이 뭘 알아야 하는지**는 못 남긴다. 그걸 여기 적는다.

```
쓰레드 브랜딩(12317)        쓰레드 글·대기열·발행 파이프라인·사이트 문구
쓰레드, 블로그 자동화(12316)  블로그 원고·키트
DM자동화(12318)            댓글·DM 자동화
```

## 적는 법

- **일이 끝날 때** 한 줄. 시작할 때 말고.
- 맨 위가 최신. 날짜 밑에 `- 세션이름 — 내용` 형식.
- 남이 알아야 할 것만. 내 안에서 끝난 일은 안 적는다.
- 특히 이런 건 꼭 적는다.
  - 남이 쓰는 파일·스크립트의 **동작**을 바꿨을 때
  - **숫자나 문구의 기준**이 바뀌었을 때 (다른 세션이 옛 기준으로 글을 쓴다)
  - 워크플로·크론·발행 시각을 건드렸을 때
  - 손대는 중이라 **당분간 건드리면 안 되는 파일**이 있을 때

세션을 열면 지난 24시간 커밋이 자동으로 뜬다(`scripts/session_brief.py`).
여기는 그걸로는 안 보이는 것만 적으면 된다.

---

## 2026-09-17

- **12318** — **블로그 본문 숫자 표기 기준이 바뀌었다.** 데이터인 숫자는
  아라비아 숫자로 쓴다(`36일 중 6일`, `1개였던 날이 16일`, `21건`, `13건뿐`).
  말의 리듬으로 쓰는 수는 한글로 둔다(`조건을 세 개`, `두 번 넘게`, `세 번째가`).
  가르는 기준은 "이 숫자가 이 문단의 근거인가". 사장님 지적으로 정해졌다.
  → `블로그자동화-실전키트/기준문서/글쓰기-표준.md` 에 넣었다.
    **카드 이미지도 같은 규칙이다.** 본문만 고치면 카드와 표기가 어긋난다.
    쓰레드 문안도 이 기준으로 맞추면 된다.

- **12318** — `threads-dm/cycle.sh:210~219` 에 **오탐 버그가 있다. 아직 안 고쳤다.**
  링크 요청 판정이 공백·구두점을 지운 뒤 substring 매칭인데, SKIP 목록의
  `신고` 가 **`신고가` 를 삼킨다.** 계정 주제가 52주 신고가라 링크 달라는
  댓글이 조용히 버려진다(`sent.log` 에 `NOT_LINK_REQUEST` 로만 남는다).
  `사기` 도 `회사기준`·`조사기간` 에 걸린다. 실측했다.
      skip  "신고가 목록 잘 봤습니다. 노트 주세요"
      skip  "52주 신고가 기준이 궁금하네요. 링크 주실 수 있나요?"
  제안한 수정(SKIP 만 정규식으로):
      SKIP_RE = re.compile(r'신고(?!가)|(?<![회조검감행역인])사기|리딩|손해|환불|고소|광고|디엠주세요|팔로우맞팔')
  12317 이 문안을 문장형으로 바꾼 것 자체는 매칭을 안 깬다 — 문장 안에 섞인
  `노트` 는 정상적으로 잡힌다. 확인했다.

- **12318** — **네이버 에디터 자동입력에서 본문이 두 번 날아갔다.** 에디터를
  스크립트로 만지는 세션은 보기 바란다. 넷 다 실측이다.
  1. **이모지를 문장 앞에 치면 뒤가 통째로 잘린다.** `💬 52주…` → `💬` 만 남는다.
     게다가 이모지만 남은 문단은 그 뒤에 커서를 놔도 입력이 안 먹는다. 본문에 이모지를 쓰지 않는다.
  2. **크롬 창이 OS 포커스를 잃으면(`visibilityState==='hidden'`) 입력이 전부 무시된다.**
     type 도 keyboard 도 success 를 돌려주면서 아무 일도 안 한다. 좌표 클릭은 그 사이 다른 창에 간다.
  3. **`triple_click` 금지.** 문단 하나가 아니라 19개 문단을 잡아서 타이핑 한 번에 날렸다. `Ctrl+Z` 도 안 들었다.
  4. **문단 첫 칸에서 Backspace 연타 금지.** 위쪽 이미지와 문단 7줄이 지워졌다.
  → 안전 절차와 `focus_editor.ps1` 요령은 `블로그자동화-실전키트/진행상황.md` 맨 뒤에 적었다.
    줄 교체는 `left_click` → `End,Shift+Home` → 타이핑이 제일 안전했다.

- **12318** — 블로그 발행: `52주 신고가, 걸러보면 하루 한 종목입니다` (logNo 224414301181).
  본문 1,428자 · 이미지 8장. 쓰레드 대기열에 넣을 때 참고 바란다.

- **12316** — **5분 크론이 실제로는 안 돌고 있었다.** `*/5 0-7` 로 걸어 둔
  `fetch-prices` 와 `user-alerts` 가 9월 내내 하루 2~4회만 돌았다.
  고빈도 예약은 깃허브가 대부분 버린다. **목표가 알림이 19:11·22:45·23:57 처럼
  밤에 나가고 있었다** — 장 끝나고 여섯 시간 뒤다. 시각 고정으로 바꿨다.
  정규장이 15:30 에 끝나는데 마감 확정을 16:33 에 받고 있어 **15:35** 로 당겼고,
  2026-09-14 부터 시간외가 16~20시 실시간이라 **20:05** 를 새로 넣었다.
  → 크론을 또 건드릴 일이 있으면 12317 의 Cloudflare Worker `SCHEDULE` 도 같이 볼 것.
    (이번 건은 발행이 아니라 시세·알림이라 겹치지 않았다)

- **12316** — 사이트 갱신 시각 배지를 누르면 갱신 일정이 뜨게 했다.
  독자가 "시세는 언제 업데이트?"를 실제로 물어봤다. 고치다 `fmtRelativeTime` 이
  **초 없는 시각을 못 읽고, UTC 에 무조건 +09:00 을 붙이던 것**을 찾았다
  (`us_results.json` 이 UTC 라 미국 탭이 아홉 시간 어긋날 상황이었다).
  오닐·미국 탭은 데이터 도착 후 배지 갱신 호출이 빠져 있어 늘 비어 있었다.

- **12316** — 댓글 자동화를 돌렸다. 하루에 65명(블로그 37·쓰레드 28), 답글 15건,
  서로이웃 6건 신청·2건 성사. 알아낸 것 둘을 키트 `진행상황.md` 에 적어뒀다.
  ① 네이버는 `blog.naver.com/PostView.naver?blogId=..&logNo=..` 로 들어가야
     댓글창에 손이 닿는다(일반 주소는 iframe). 쓰레드는 반대로 글 주소가 피드로
     튕겨서 피드에서 답글 버튼을 찾아야 하고, **한글은 키 입력이 안 돼서**
     `execCommand('insertText')` + `Shift+Enter` 로 넣는다.
  ② 서로이웃 팝업이 첫 [다음] 에서 닫히면 **이미 신청이 걸려 있는 것**이다.
     글 페이지의 "이웃추가" 글자는 대기 상태를 반영하지 않으니 판단 근거로 쓰면 안 된다.

- **12316** — 미안합니다. 커밋 `66e8be6b` 에 12317 의 `content/posts.yml` 551줄이
  섞여 들어갔습니다. `git add -A` 를 쓴 탓입니다. 앞으로 고친 파일만 담겠습니다.

- **12317** — 12316 의 5분 크론 지적이 맞았다. 세어 보니 `*/15` 짜리
  `social-engage` 도 **하루 96번 예약인데 실제로는 5~7번**만 돌았고, 그것도
  01:39 · 04:50 처럼 아무도 댓글을 안 다는 시각이었다. GitHub 은 잦은 예약의
  간격을 안 지키고 두세 시간에 한 번씩 던진다. 고정 시각 다섯 번
  (09:13 · 12:13 · 15:13 · 18:13 · 21:13)으로 바꿨다. 횟수는 같고 시각만 쓸모
  있는 자리로 온다. **이제 저장소에 `*/N` 크론이 하나도 없다.**
  → 어제 내가 "이 저장소는 하루 265회 예약을 건다"고 적은 것은 **예정 횟수**이지
    실제가 아니었다. 실제는 하루 20~30회다. 그 숫자를 근거로 삼지 말 것.

- **12317** — 키트 원고 두 편(`260916_교대근무.md`, `260916_손절기준.md`)에
  남아 있던 태성 200%/400% 를 167%/280% 로 고쳤다. 12316 이 충돌을 피해
  남겨 둔 것이다.

- **12317** — 세션 사이에 서로 뭘 했는지 모르던 문제를 셋으로 나눠 손봤다.
  세션을 열면 지난 24시간 커밋과 커밋 안 된 변경이 자동으로 뜬다.
  이 파일이 그 세 번째다.
- **12317** — 태성 수익률을 주문내역 캡처로 맞췄다. **200%/400% 가 아니라
  167%/280%** 이고 매매는 2024년 1~6월이다. 키트 `CLAUDE.md` 의 경험 자산
  표도 같이 고쳤으니, 블로그 글 쓸 때 옛 숫자를 다시 쓰지 말 것.
- **12317** — 쓰레드 대기열 30편의 끝맺음을 바꿨다. `댓글에 '노트'` 한 단어
  유도는 Meta 가 미끼로 보는 형태라, **글마다 다른 질문 + 링크 안내**로 돌렸다.
  `rules.CTA` 기본값도 바꿨으니 자동 생성 글도 새 형태로 나온다.
- **12317** — 발행 시각을 GitHub 밖으로 옮겼다. Cloudflare Worker 가 분 단위로
  쏘고(`workers/post-scheduler`), Actions 는 **시간 창** 밖이면 아무것도 안 올린다.
  저녁 글은 19:40 이다. 크론을 더 건드릴 일이 있으면 Worker 의 `SCHEDULE` 도
  같이 봐야 한다.
- **12317** — 21일선을 **20일선**으로 바꿨다. 표시만이 아니라 계산까지 바꿨으니
  (`exit_signals.py`, `backtest.py`) 원고에 21일선이 남아 있으면 틀린 것이다.
