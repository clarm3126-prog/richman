# 작업일지

세션 셋이 같은 저장소를 만진다. 커밋 메시지는 **무엇을** 바꿨는지는 남기지만
**왜** 그랬는지, 그래서 **다른 세션이 뭘 알아야 하는지**는 못 남긴다. 그걸 여기 적는다.

```
쓰레드 브랜딩(12317)        쓰레드 글·대기열·발행 파이프라인·사이트 문구·블로그 원고
쓰레드, 블로그 자동화(12316)  댓글 자동화 (네이버 블로그 댓글 + 쓰레드 답글)
DM자동화(12318)            DM 자동화
```

## 적는 법

- **일이 끝날 때** 한 줄. 시작할 때 말고.
- 맨 위가 최신. 날짜 밑에 `- 세션이름 — 내용` 형식.
- 남이 알아야 할 것만. 내 안에서 끝난 일은 안 적는다.
- 특히 이런 건 꼭 적는다.
  - 남이 쓰는 파일·스크립트의 **동작**을 바꿨을 때
  - **숫자나 문구의 기준**이 바뀌었을 때 (다른 세션이 옛 기준으로 글을 쓴다)
  - 워크플로·크론·발행 시각을 건드렸을 때
  - 손대는 중이라 **당분간 건드리면 안 되는 파일**이 있을 때

세션을 열면 지난 24시간 커밋이 자동으로 뜬다(`scripts/session_brief.py`).
여기는 그걸로는 안 보이는 것만 적으면 된다.

**이 파일은 셋이 같이 쓴다.** `git add docs/작업일지.md` 를 하면 그 순간
남이 쓰던 줄까지 같이 담긴다. `git add -A` 를 안 쓰는 것으로는 못 막는다 —
파일을 골라 담아도 같은 일이 난다(2026-09-20, `f870a912` 에 12318 항목이
딸려 들어갔다. 내용은 그대로라 손해는 없었다).

그래서 이렇게 한다.

- **적었으면 바로 커밋한다.** 오래 들고 있을수록 남의 커밋에 실린다.
- 남의 항목이 내 커밋에 실려도 **되돌리지 않는다.** 내용이 남아 있으면 된
  것이고, 되돌리려다 지우는 쪽이 더 나쁘다. 실렸다는 것만 서로 알리면 된다.

---

## 2026-09-24

- **12316** — 블로그 댓글 10건을 더 달았다(오늘 두 번째 회차, 누적 92곳 · 단글 102건).
  `namchi` · `garden_for_wellness` · `schuft5` · `tnt6328` · `ksiplan` ·
  `stock_life` · `living8080` · `bogilwoman` · `wsjsid` · `kdslkoiut`.
  `stone1244` 는 관심테마 TOP3 라 걸렀다.
- **12316** — 🚨 **12318 이 알려준 고장이 내 수집기에도 둘 있었다. 고쳤다** (키트 `606a864`).
  저쪽은 댓글 본문을 뽑는 JS 가 깨져 본문이 늘 비었고, 판정하는 쪽이 그 빈 값을 보고
  `링크 요청 아님` 으로 적었다. **16건 전부 읽은 적이 없는데 `done` 이 됐다.**
  - `댓글수집.mjs` — `본문()` 이 `se-main-container` 를 못 찾거나 **사진만 올린 글**이면
    빈 문자열을 돌려줬다. `catch` 에도 안 걸리니 부르는 쪽은 그걸 본문으로 알았다.
    실패는 `null` 로 주고, 못 읽었으면 **요약으로 슬쩍 바꿔치지 않는다.**
    `본문읽음` 을 같이 적어 "읽었는데 짧은 글"과 "못 읽은 글"을 가른다.
  - `블로그찾기.mjs` — `p.contents` 가 비면 제목에만 주식 낱말이 없을 때
    **`주식아님` 으로 세고 버렸다.** 거른 것과 못 읽은 것은 다르다. 이제 따로 센다.
  - 오늘 `reviewtrading` 글이 실제로 본문이 비어 있었다(이미지 글). 눈으로 보고
    건너뛰었고 `상태.json` 에 `_본문못읽음` 으로 적었다. **거름으로 적지 않았다** —
    거름으로 적으면 다시는 안 보게 된다.
  - 오늘 돌린 결과는 `본문못읽음 0` 이라 이번엔 안 물렸다. **고장은 안 났어도 자리는 있었다.**


- **12317** — 📏 **시세 갱신을 다시 셌다. 발행본 약속을 지킨다.** 결론이 바뀌었다.
  발행본 `224417798337` 「5분마다 갱신된다고 적어놨는데, 세어보니 하루 세 번이었습니다」
  본문에 **"다음 주에 다시 세어서 적겠습니다"** 가 나가 있다.
  - **하루 세 번 → 다섯 번.** 9/17 에 크론을 두 시간 이상 벌린 뒤(`0ede7c6a`) **다섯 칸이
    다 돈다.** 9/16 이전에는 2~4번이었다. 그 고침이 먹었다.
  - ⚠️ **그런데 전부 네다섯 시간 늦는다.** 예약 시각은 사실상 뜻이 없다.
    ```
    예약        실제(KST)        늦은 중앙값
    09:10  →  13:50 쯤           +280분
    11:10  →  16:42 쯤           +332분
    13:10  →  18:17 쯤           +307분
    15:35  →  21:05 쯤           +330분
    20:05  →  다음날 00:31 쯤    +266분
    ```
  - **"오전 시세가 없다"는 12318 말이 맞았다.** 다만 까닭이 "실행이 없다"가 아니라
    **"개장 직후로 걸어둔 것이 13:50 에 온다"** 다. 장이 열린 아침에는 어제 값이 떠 있다.
  - ⭐ **오늘 고친 스냅샷 날짜 버그의 뿌리가 여기다.** `20:05` 칸이 KST 00:31 에 닿으니
    `datetime.now()` 가 다음 날이 됐다. 늦는 것을 못 고치니 **이름을 시계에서 떼어낸** 것이
    맞는 대처였다.
  - **처음엔 KST 날짜로 묶어 세어 "하루 4번"이 나왔다.** UTC 하루의 다섯 실행이 KST
    로는 두 날에 걸친다. 예약이 UTC 로 걸려 있으니 UTC 로 세야 한다. **또 모집단이었다.**
  - 곁가지: **9/11(금)·9/12 에 시세 커밋이 아예 없다.** 9/10 07:27 다음이 9/14 02:45 이다.
    사흘 구멍이고, 스냅샷 보관함의 빈 8일 가운데 일부가 이것으로 보인다. 옛일이라 뒀다.

- **12317** — 12318 이 준 규칙을 내 코드에 댔다. **같은 자리가 셋 있었다.**
  > **빈 값을 "아니다"로 읽지 않는다.** 입력이 비었으면 판정을 미루고 흔적을 남기지 않는다.

  그쪽 판정기가 댓글 본문을 못 읽고도 `링크 요청 아님` 으로 **16건 전부** 적어, 그 사람들이
  `sent.log` 에 들어가 영영 제외됐다(퍼머링크를 안 남겨 되짚을 수도 없다). 내 쪽에도 있었다.
  ```
  workflow_commit_check  파싱 실패 → set() → 부르는 쪽은 "쓰는 게 없다"로 읽음
  stale_terms            파일 못 읽으면 조용히 건너뜀 → "이상 없음"이 뜸
  ma_5060_compare        깨진 하루치를 조용히 빼 → 날수만 줄고 까닭이 안 나옴
  ```
  셋 다 **못 본 것을 없는 것으로** 말했다. 이제 셋 다 소리를 낸다.
  - `workflow_commit_check` 에 `UNPARSED` 를 뒀다. 일부러 스크립트 하나를 깨뜨려
    확인했다 — `파싱이 안 되는 스크립트 1개 — 이 파일들에 대해서는 아무 말도 못 합니다`
    가 뜨고, 같은 워크플로가 「담는데 쓰는 걸 못 찾음」으로도 한 번 더 걸렸다.
  - `stale_terms` 는 못 읽은 파일이 있으면 **종료코드 1** 로 알린다. 세션 브리프에 뜬다.
  - ⭐ **이것이 오늘의 규칙이다.** 「아무 소리도 안 나는 고장」의 가장 흔한 꼴이
    **빈 입력으로 내린 판정**이다. 조용하고, 기록에 남으면 되돌릴 수 없다.

- **12316** — 쓰레드 댓글 10건을 달았다. **그 과정에서 같은 글에 3개를 달았다가 2개를 지웠다.**
  블로그 10건은 앞서 끝냈다(`qaz10814` 외 9곳, 누적 82곳 · 단글 92건).
  - 단 곳 — `letsgrit_` · `prof_leverage7` · `alzza_baegi` · `tei.chae` · `henley_kim_` ·
    `b_j_s` · `pinkfish76` · `pnx.trading` · `amisyoori` · `solsolsdiary`
  - 거른 곳 8 — 종목 찍기(`joosang3` · `swingstock7`), 리딩 시그널(`strikersquantlab_j` ·
    `300e_trader`), 코인 방향 예측(`junhxunu` · `heaju_coin`), 주식 글 아님(2)
- **12316** — 🚨 **게시물 페이지가 내 답글을 안 보여준다. `아직 답글이 없습니다` 라고까지 쓴다.**
  작업요령에 이미 "다시 누르지 마라"라고 적어놨는데 내가 그 페이지를 믿고 다섯 번 눌렀다.
  **세 번이 올라가서** `@prof_leverage7` 에 같은 댓글이 3개 달렸다. 두 개 지웠다.
  - 2026-09-20 `@dongnam__` 에서 똑같이 3개를 달았다. **같은 고장을 나흘 만에 되풀이했다.**
  - **아무 소리도 안 나는 고장이다.** 등록은 성공하는데 화면이 실패처럼 보인다.
    에러도 토스트도 없다. 콘솔에 `Instance key not found for creation source:
    create_reply_...` 가 뜨지만 이건 등록 실패가 아니라 화면 갱신이 깨진 흔적이다.
  - **확인은 `/@jongmoknote/replies` 에서 `location.reload()` 하고 6초 기다려 센다.**
    `chrome_navigate` 만으로는 캐시된 옛 목록이 나온다. 5초로는 모자랐던 적이 있다.
  - **등록은 `게시` 버튼 좌표 클릭이 제일 안정적이다.** 글이 들어 있을 때는 w60·h34 로
    제대로 잡힌다("0×0 이라 못 누른다"는 옛 기록은 빈 칸일 때 이야기였다).
    `Enter` 는 되기도 하고 안 되기도 한다 — `@alzza_baegi` 는 Enter 두 번이 다 안 먹고
    `게시` 클릭 한 번에 올라갔다.
  - 삭제도 같다. 지워도 화면은 그대로다. **하나 지우고 새로고침해서 개수를 세야** 한다.
    확인창의 `삭제` 도 좌표 클릭이어야 먹었다(JS `.click()` 은 안 먹었다).
- **12316** — 후보를 고르기 전에 `상태.json` 의 `쓰레드별마지막` 을 안 읽어서
  `@bamsong.trading` 글을 열었다가 **4일 전 내 댓글**을 봤다. 달기 전에 알아챘다.
  `pnx.trading` 은 09-20 에 다른 글에 단 적이 있는데 그건 모르고 달았다(글은 다르다).
- **12316** — **연휴엔 추천 피드가 마른다.** 홈 추천이 4건에서 더 안 불어났다
  (`scrollHeight` 가 안 늘었다). 검색이 `매매일지` 11건 · `손절` 10건으로 더 많았다.
  피드 우선 원칙은 그대로 두되, **연휴에는 네 건 쓰고 바로 검색으로 넘어간다.**
- **12316** — 숫자는 `data/stop_sensitivity.json` 을 그날 열어서 썼다.
  **-5% 952건 · 손절 81.4% · 승률 14.4% / -15% 손절 64.8% · 승률 23.9%** (`updated 09-23 23:24`).


- **12317** — 추석(9/24~25) 휴장일 발행을 확인했다. **거의 다 맞게 돌고, 한 군데만 고쳤다.**
  평일 크론이 그대로 돌기 때문에 공휴일도 발행 일정은 살아 있다.
  ```
  08:09 us_brief   나감 ✅  미국장은 열렸다. 오늘 이미 나갔다
  16:08 kr_close   안 나감 ✅  compose_kr_close 에 이미 가드가 있다
                            (trading_day != today → None)
  18:40 auto       나감 — 제목만 고쳤다
  19:40 queue      나감 ✅  대기열 글은 날짜에 안 묶인다 (오늘은 태성 매매 기록)
  ```
  - **고친 것**: `auto` 글 제목이 `오늘의 미너비니 조건 통과 종목` 인데 바로 아랫줄이
    `9월 23일 기준` 이었다. **숫자는 맞는데 제목과 어긋난다.** 읽는 사람이 두 줄을
    스스로 맞춰야 한다. 장날이 오늘이 아니면 "오늘의"를 뗀다.
    날짜는 아랫줄에 맡긴다 — 제목에도 넣으면 같은 날짜가 두 줄 연달아 나온다.
  - `_stale(max_age_days=4)` 는 그대로 둔다. **나흘까지 봐주는 건 맞는 설계다** —
    스크리너가 한 번 실패해도 계정이 조용해지지 않는다. 문제는 낡음이 아니라
    **언제 것인지를 제목이 숨긴 것**이었다.
  - 모멘텀·테마 제목에는 "오늘"이 없어 안 고쳤다.

- **12317** — 🚨 **스냅샷 이름이 장날이 아니라 「돌린 날짜」로 붙고 있었다. 고쳤다.**
  월요일 점검을 하다 찾았다. 9/21(월) 자료가 `20260922.json` 으로 들어가 있었다.
  - **까닭**: `datetime.now(KST)` 로 파일 이름을 지었다. 크론은 17:07 인데 깃허브가
    몇 시간 늦게 돌려 자정을 넘기면 하루가 밀린다. 그러면 이튿날 진짜 그 이름으로
    저장하려 할 때 `if dst.exists(): return` 에 막혀 **그날 자료가 조용히 버려진다.**
    에러도 로그도 없다.
  - ⚠️ **더 나쁜 것이 있었다 — 같은 장날이 여러 파일로 남아 있었다.**
    장중에 돌면 네이버가 아직 어제 종가를 준다. `0910`·`0911`·`0913` 세 파일이
    **모두 9/10 자료**였다. 백테스트는 파일마다 하루로 세므로 **그날 종목이 세 번
    계산됐다.** 겹쳐서 지운 6일이 미너비니 **176건** · 모멘텀 **147건**이다
    (`backtest_stats` 의 952건 가운데).
  - **고친 방법**: 이름은 못 믿지만 주가는 못 속인다. 스냅샷 안의 종목 100개를
    `data/charts/` 의 일별 종가와 맞춰 다수로 장날을 정했다. 96~100개가 일치해
    판정이 또렷했다. **맞은 종목이 30개도 안 되는 셋(`0512`·`0914`·`0915`)은 손대지
    않았다** — 장중 값이라 어느 종가와도 안 맞는 것으로 보인다. 확실하지 않으면 둔다.
    ```
    파일 92개 → 86개 (겹친 6개 지움) · 이름 5개 바꿈
    거래일 아닌 이름 6개 → 0개
    빈 거래일 8일은 그대로 (애초에 안 받아온 날)
    ```
  - 바꾼 파일에 `_renamed_from` 과 까닭을 남겼다. `index.json` 도 다시 썼다 —
    그대로 두면 **화면 날짜 고르개가 없는 날을 가리켜 빈 화면이 된다.**
  - **앞으로**: `screener.py` 에 `set_trading_day(histories)` / `trading_day()` 를 뒀다.
    받아온 시세의 마지막 날짜를 장날로 쓴다. `momentum_screener.py` 도 **같은 함수**를
    쓴다 — 두 보관함이 같은 날을 가리켜야 backtest 가 짝을 맞춘다. 건너뛸 때 소리도 낸다.
  - **⚠️ 12316·12318 보실 것**: `backtest_stats.json` 의 숫자가 **오늘 밤 backtest 가
    돌면 바뀝니다.** 952건·승률 14.4% 가 겹친 날을 빼고 다시 나옵니다. 그때까지
    인용을 미루시거나, 인용하실 때 "9/24 정정 전 값"임을 아셔야 합니다.

- **12317** — 월요일 점검 결과: **스크리너 쪽은 사흘 다 정상.** 차트 749/798/728개
  (전 200개), 5~7월에 멈춘 것 478 → 387개, `avg_volume.json` 725종목 날마다,
  60일선 그림자 2일치, 추세 판정 `ttn` 분포 그대로, `entry_days` 양쪽 58 로 일치.
  - ⛔ **제가 9/20 에 드린 "거래량 평균이 주간 값과 20.1% 차이" 는 틀렸습니다.**
    낡은 차트로 쟀습니다. **실제 중앙값 6.8%**, 상위 10% 가 36% 입니다.
  - 차트 용량은 걱정보다 가볍다. 폴더 전체 19MB (9.2MB/일로 늘 거라고 했는데
    하루치가 뒤에 붙는 꼴이라 git 델타가 잘 먹는다).

- **12317** — 🔴 **DM 답글 경로 0건의 까닭은 「아무도 안 왔다」가 아니다.**
  재부팅 뒤 `cycle.log` 에 `탭 확보 실패` 가 **여섯 번** 찍혀 있다. `sent.log` 도
  9/20 의 51줄에서 한 줄도 안 늘었다. **`./reconnect.sh` 를 안 돌려 죽은 탭을
  가리키고 있다** — 12318 이 재부팅 전에 일지에 적어 둔 그것이다.
  - → 조건표에 한 칸이 더 필요하다. 지금 표는 **수집이 됐다는 전제** 위에 서 있다.
    `cycle.log` 에 `탭 확보 실패` 가 있으면 **표식·기록 판정을 믿을 수 없다.**
  - 사장님께 `./reconnect.sh` 를 올렸다. 사장님 크롬이 필요해 내가 못 한다.

## 2026-09-20
- **12316** — **댓글 기록을 저장소 밖으로 옮겼다.** 사장님 지시다.
  `%LOCALAPPDATA%\종목노트\댓글기록\` 에 `상태.json`·`대상.json`·`새블로그.json`·
  `큐데이터.json`·초안 md·큐 html 이 있다. 남의 블로그 아이디와 그 사람에 대한 판단
  (거른 이유)이 들어 있어서, 저장소가 공개인 한 `.gitignore` 한 줄에 기댈 수 없다.
  `scripts/블로그찾기.mjs`·`댓글수집.mjs` 의 `기록집` 한 줄만 고치면 경로가 바뀐다.
  → **저장소 `블로그자동화-실전키트/댓글/` 에 기록 파일을 다시 만들지 말 것.**

- **12316** — **`블로그찾기.mjs` 의 중복 거르기가 통째로 안 먹고 있었다.**
  `단글`(배열)을 `Object.keys()` 로 읽어서 `["0","1","2"]` 가 나왔다.
  **에러도 안 나고 Set 크기도 82로 같아서** 도는 것처럼 보였다. 이것 때문에
  같은 글에 두 번 댓글이 5건 나갔다. 고친 뒤 "이미봄"이 1건 → 42건으로 잡힌다.
  → 고치고 나서 **일부러 걸리는 경우를 하나 넣어 돌려 확인했다**(12317 조언).
     `고친 코드 true / 옛 코드 false / Set 크기 둘 다 82`.

- **12316** — **쓰레드 답글 등록은 `Enter` 한 번이다. 재시도하면 중복이 난다.**
  등록 버튼의 클릭 영역이 0×0 이라 좌표가 안 먹는다. 그리고 쓰레드는 **방금 단 답글을
  그 페이지에 바로 안 그린다.** "안 올라갔다"고 판단해 세 번 눌러 같은 댓글이 3개 달렸다
  (2건 삭제). 확인은 `/@jongmoknote/replies` 에서만 한다.

- **12316** — **팔로우 버튼 글자가 방향을 알려준다.** 이걸 거꾸로 읽어 12318 에게
  "DM 경로가 열렸다" 고 잘못 알렸다. 쓰레드 DM 은 **그 사람이 나를 팔로우해야** 열린다.
  ```
  팔로우    아무도 서로 안 따른다
  맞팔로우  그 사람이 나를 먼저 팔로우했다   ← DM 경로가 열리는 쪽
  팔로잉    내가 이미 따르고 있다
  ```
  → 세션끼리 팔로우 얘기를 할 땐 **"내가 걔를" 인지 "걔가 나를" 인지 같이 쓴다.**

- **12316** — **쓰레드 검색이 리딩 스팸에 잠식됐다.** 계정 열둘이 같은 문구를 복붙한다.
  `손절 기준` 0건 · `계좌 정리` 는 보험·사주만 나온다. **사장님 제안으로 추천 피드(홈)를
  먼저 훑는 쪽으로 바꿨다.** 한 번에 28건, 그중 새 사람 넷을 바로 찾았다. 검색은 0~2건이었다.
  스팸 문구 여덟 개는 12318 에게 넘겼고 여섯 개가 `SKIP_RE` 에 들어갔다.

- **12316** — 오늘 댓글 **블로그 32 · 쓰레드 40건**(누적 블로그 72 · 쓰레드 77).
  서로이웃 신청 4 · 일방 이웃 2 · 팔로우 29. **09-20 부터 등록 버튼도 내가 누른다**
  (사장님 지시, 정본 `CLAUDE.md` §4(8) 반영). 대신 경험 문장은 정본 §4 에 적힌 것만 쓰고
  숫자는 그날 `data/*.json` 에서 직접 읽는다.

- **12316** — ⚠️ **오전에 낡은 백테스트 숫자가 쓰레드 답글로 나갔다.**
  키트 사본 `CLAUDE.md` 의 `907건·82.9%·13.1%` 를 인용했는데 정본은 이미
  `918건·82.4%·13.6%` 였다. 사장님 판단으로 답글은 안 지웠다(결론이 안 바뀐다).
  → **사본 `CLAUDE.md` 를 숫자 출처로 쓰지 않는다.** `data/*.json` 을 그날 읽는다.



- **12317** — 검사기에 **`--prove`** 를 붙였다. 12318 이 쓴 방법을 자에 넣은 것이다.
  훑어서 0곳이 나오는 건 여전히 "못 잡아서"일 수 있다. **일부러 망가뜨려야 안다.**
  워크플로마다 `git add` 줄을 메모리에서 지워 보고 이 자가 짚는지 센다(파일은 안 건드린다).
  ```
  담는 줄이 있는 워크플로 18개 가운데 전부 잡습니다
  ```
  - **「담는데 쓰는 걸 못 본」 워크플로도 이제 소리를 낸다.** 무언가를 커밋한다면
    누군가는 그걸 쓴다. 쓰기가 안 보이면 **그 워크플로에 대해서는 아무 말도 할 수 없는
    상태**인데, 전에는 그걸 "이상 없음"으로 읽었다. `social-engage`·`social-post` 가
    딱 그랬다. 지금은 0개다.
  - 이제 이 자에 대해 세 가지를 말할 수 있다.
    `--test` 17/17 (쓰는 쪽 11 · 담는 쪽 6) · `--prove` 18/18 · 훑기 0곳.
  - 12318 이 따로 확인해 준 것: `social-check`·`visit-summary` 는 `write_text`·`json.dump`·
    `open(...,'w')`·`save_json` 이 **한 곳도 없는 보고 전용**이라 비는 게 맞다.
    `fetch-prices` 의 `쓴다 9 · 담는다 1` 은 그 하나가 `data/` 라 아홉을 다 덮는다.

- **12317** — 🚨 **검사기가 12318 워크플로 둘에 대해 "조용했는데 통과가 아니라 안 본 것"이었다.**
  12318 이 워크플로마다 `쓴다 / 담는다` 를 나란히 찍어 잡아냈다.
  ```
  social-engage.yml   쓴다 set()   담는다 set()   ← 양쪽이 다 비면 구멍이 나올 수가 없다
  social-post.yml     쓴다 set()   담는다 set()
  ```
  원인이 **둘 다**였다. 한쪽만 고쳤으면 그 워크플로의 모든 경로가 헛걸렸다(56곳 때와 같은 꼴).
  - **담는 쪽**: 정규식이 `for f in` 이라 `for p in data/social; do` 를 못 읽었다.
    루프 변수 이름은 아무거나 될 수 있다. `git add "$p"` 도 그냥 지나갔다.
    (`2>/dev/null` 이 `/` 만 보고 섞여 들던 것도 같이 고쳤다)
  - **쓰는 쪽**: `from social import store` 꼴을 안 이었다. `ImportFrom` 의 `module` 이
    `social` 이고 `names` 가 **모듈 이름**인 꼴이다. `data/social` 쓰기가 거기 있었다.
  - **`--map` 을 붙였다.** 워크플로마다 `쓴다/담는다` 를 찍는다.
    **양쪽이 다 비면 통과가 아니다** — 안 본 것일 수 있다. 12318 이 쓴 진단법을 자에 넣었다.
  - `--test` 를 **담는 쪽까지** 시험하게 넓혔다(17개). 전에는 쓰는 쪽만 봐서 반쪽이었다.
  - `assets/` 도 본다(카드 이미지). 다만 **`card.py` 처럼 경로를 인자로 받아 쓰는 함수는
    여전히 못 본다.** 머리말에 적어 뒀다 — 못 보는 것을 아는 채로 쓰는 것과 모르는 것은 다르다.
  - 지금: 시험 17/17 · 전체 훑기 0곳 · 어제 상태로 되돌리면 두 곳을 잡는다 ·
    `--map` 에서 양쪽이 빈 것은 `social-check`·`visit-summary` 둘뿐이고 **둘 다 `git add` 가
    아예 없는 점검 전용 워크플로**라 비는 게 맞다.

- **12317** — 12318 교차검증 결과 셋을 반영했다. **검사기가 옆 세션 코드를 통째로 못 보고 있었다.**
  - 🚨 **`workflow_commit_check.py` 가 침묵한 건 이상이 없어서가 아니라 보지를 않아서였다.**
    12318 이 잡아 줬다. `Path("data/...")` 로 **시작하는 것만** 따라가서
    `ROOT / "data" / "social"`(12318 방식)도 `save_json(파일, ...)`(telegram_link 방식)도
    안 보였다. **그쪽 워크플로가 안전한 건 사실이었지만 이 자의 침묵은 그 근거가 아니었다.**
    - 고치는 과정에서 **반대로 56곳을 헛걸었다.** 불러온 모듈을 통째로 따라가서,
      `earnings.py` 가 `screener` 를 부른다는 이유로 스크리너의 모든 저장이 딸려 왔다.
      그다음엔 부르는 **이름**만 맞춰 이었더니 `main` 이 어디에나 있어 다섯이 걸렸다.
    - 지금은 **어느 모듈에서 가져왔는지까지** 본다(`from X import f` / `X.f()`).
      `if [ -e x ]; then git add x; fi` 도 읽는다(세미콜론 탓에 하나가 헛걸렸다).
    - **`--test` 를 붙였다.** 열 가지 쓰기 꼴을 스스로 시험한다. 규칙을 고쳤으면 돌려라.
      지금: 시험 10/10 · 전체 훑기 0곳 · 어제 상태로 되돌리면 두 곳을 잡는다.
  - ⚠️ **1.5배 문턱도 새 분모를 쓴다 — 배수만 바뀌는 게 아니다** (12318 지적).
    최근에 거래량이 터진 종목은 분모가 커져 **문턱을 못 넘는다.**
    **09-20 전후로 돌파 건수를 비교하면 "돌파가 줄었다"로 잘못 읽힌다.** 목록의 구성이
    달라진 것이다. `fetch_prices.py` 그 자리에 주석으로 박았다.
  - `save_avg_volume` 이 **빈 파일을 덮어쓰던 것**을 막았다. 시세를 하나도 못 받은 날
    전 종목이 주간 값으로 물러서는데 **아무 소리도 안 났다.** 옛 파일을 남기고 소리를 낸다.
  - ✅ 12318 이 직접 확인해 준 것: `save_chart_data` 문자열/dict 둘 다 안전 ·
    `fetch_prices` 물러서기 정상 · `screener.yml` YAML 정상 ·
    **백분위를 다른 방법으로 다시 세서 삼성전자 "위에서 29%" 일치**(그쪽이 -27% 를 얻었던 자리다)

- **12317 · 12316 · 12318 공통** — 🔇 **「아무 소리도 안 나는 고장」 — 오늘 일곱 번 걸렸다.**
  셋이 따로 겪고 따로 고쳤는데 같은 것이었다. 이름을 붙여 둔다.
  ```
  중앙값 -27% vs -44%    조건 통과 200개를 셌다 (전 종목은 2,666개)
  907건 vs 918건          키트 사본을 봤다 (정본은 고쳐져 있었다)
  entry_days 57 vs 54     평가 전 픽으로 날짜를 셌다 (건수는 평가 후)
  11월 vs 12-27           40일 쌓이는 날만 셌다 (그 40일이 30일을 채워야 한다)
  차트 478개              파일은 5월인데 화면은 오늘인 척 그렸다
  보관함 누락             만들어지는데 워크플로 git add 목록에 없었다
  Object.keys(배열)       인덱스를 돌려줘 Set 크기가 82로 똑같았다 (12316)
  ```
  **일곱 다 에러가 안 났다.** 빈 결과도 아니었다. 겉으로는 도는 것처럼 보였다.
  값이 틀린 게 아니라 **무엇을 세고 있는지 / 어느 때 것인지 / 어디로 가는지가 달랐다.**
  - **그래서 이렇게 한다.**
    - 값을 주고받을 때 **파일·키에 더해 그 파일의 갱신 주기와 모집단**까지 적는다.
    - 거르기·검사기를 고쳤으면 **일부러 걸리는 경우를 하나 넣어 돌려 본다.**
      "이상 없음"은 잡을 힘이 있을 때만 뜻이 있다. 실제로 내 워크플로 검사기는
      **못 잡으면서 이상 없음을 찍고 있었다.**
    - 새 파일을 만들면 **워크플로 `git add` 목록도 같이 본다** (`scripts/workflow_commit_check.py`
      가 세션 브리프에서 자동으로 센다).
    - 화면에 숫자를 내놓을 때 **어느 때 것인지를 같이 적는다** (차트 `2026-09-18까지`).

- **12317** — `entry_days` 57 vs 54 는 **월요일에 저절로 풀린다.** 기다리는 게 맞다.
  둘을 열어보니 **같은 파이프라인이 5초 간격으로 쓴 두 파일이 다른 숫자**였다
  (둘 다 `2026-09-18 22:45`). 데이터가 낡은 게 아니라 코드가 달랐던 것이고,
  그 고침은 **09-19 에 이미 들어갔다**(`a65f8518`). 그 뒤로 거래일이 없어서 파일이
  아직 안 다시 써졌을 뿐이다. 월요일 밤 backtest 가 돌면 54 로 맞춰진다.
  - 그때까지 **기간(거래일 수)은 `backtest_stats.json` 의 54** 를 쓴다. 12316 에게도 전했다.
  - **918건 · 82.4% · 13.6% 는 안 바뀐다.** 그건 그대로 인용해도 된다.

- **12317** — 차트를 **후보 전체에 날마다** 쓰고, **20일 평균 거래량을 날마다** 낸다. 사장님 지시.
  - **차트가 멈춘 까닭**: `save_chart_data(to_save, ...)` 가 **상위 200개만** 다시 썼다. 조건에서
    빠진 종목의 파일은 그대로 남는다. 그런데 시세는 후보 전체(거래대금 10억+, 오늘 838개)에
    대해 **이미 받아놨다.** 안 쓰고 버리고 있었다. 이제 전부 쓴다.
    → ⚠️ **날마다 바뀌는 차트 파일이 200개 → 838개.** 생 용량 2.2MB/일 → 9.2MB/일.
    (차트 파일은 날마다 한 줄이 붙는 꼴이라 git 델타가 잘 먹는다. 그래도 네 배다.)
  - **180일로 줄이려다 되돌렸다.** `drawCandlestickSVG` 가 **파일에 담긴 종가 전체**로
    이동평균을 낸 뒤 180일 창을 잘라 쓴다. 180일만 담으면 **200일선이 통째로 사라진다.**
    용량을 줄이려다 선을 지울 뻔했다. 252일 그대로 둔다.
  - **`data/avg_volume.json` 이 새로 생긴다** (날마다, 838종목, ~15KB).
    20일 평균 거래량은 `ath_cache.json` 에 있었는데 **그 파일이 주 1회**다. 역대 고가는 긴
    시세가 필요해 주 1회가 맞지만 20일 평균은 20일이면 된다. 이레 묵은 분모로 나누면
    배수가 부풀려진다 — 닷새 전에 터진 거래량이 아직 분모에 없기 때문이다.
    쓰는 쪽: 화면 요약의 "20일 평균의 O배", `fetch_prices.py` 의 신고가 돌파 감지.
    후보에 없는 종목은 전처럼 `ath_cache` 로 물러선다.
  - **⚠️ 12318 보실 것**: `ath_breakouts_history` 의 `vol_ratio` 가 2026-09-20 부터 날마다 낸
    분모로 바뀝니다. **정의가 바뀐 게 아니라 낡음을 고친 것**이지만, 그 전후를 한데 묶어
    셀 때는 알고 세야 합니다. 9/17 발행본의 `중앙값 3.7배` 는 이전 분모로 낸 값입니다.
  - 🐛 **곁가지로 찾은 것**: `screener.yml` 커밋 목록에 `data/screener_conditions_history` 가
    빠져 있었다. 오늘 아침에 만든 60일선 그림자 보관함이 **만들어지고 그대로 버려질
    뻔했다.** 같이 넣었다. **새 파일을 만들면 워크플로 커밋 목록도 같이 봐야 한다.**

- **12317** — 🐛 **종목 차트 1,570개 중 478개가 5~7월에서 멈춰 있는데 화면이 아무 말도 안 했다.**
  12318이 짚은 "거래량 분모가 주간 캐시"를 되짚다가, 더 싱싱한 출처를 찾으려고 `data/charts/` 를
  열어봤다가 나왔다. 가장 오래된 건 **2026-05-12**다.
  ```
  202605 138개 · 202606 157개 · 202607 183개 · 202608 308개 · 202609 784개
  ```
  - **까닭**: `screener.py:save_chart_data()` 가 **그날 결과에 든 종목만** 다시 쓴다. 조건에서
    빠진 종목의 파일은 그대로 남는다. **지우는 게 답이 아니다** — 지우면 아무것도 안 보인다.
  - **문제는 화면이 그걸 말하지 않은 것이다.** 5월에 멈춘 봉차트가 오늘 것처럼 그려졌다.
    값이 틀린 게 아니라 **어느 때 것인지를 안 밝힌** 것 — 오늘 하루 종일 다룬 것과 같은 종류다.
  - **고침**: 차트 밑에 `2026-09-18까지` 를 늘 적는다. 이레 넘게 뒤처졌으면 노란 띠로
    "그 뒤로 조건 계산 대상에 안 들어 멈춰 있습니다. 현재가와 거래량은 위쪽 값이 오늘 것입니다".
  - ⚠️ **`data/charts/` 를 싱싱한 값의 출처로 쓰면 안 된다.** `ath_cache`(주간)보다 더 낡았다.
    거래량 20일 평균을 다시 내려다 이걸로 하려 했는데 접었다.

- **12317** — 🧭 **종목 상세에 "지금 어느 자리인가" 두세 문장을 붙였다.** 사장님 지시. LLM 없이 규칙이다.
  사장님은 "AI로"라고 하셨지만 1단계는 규칙으로 가기로 정하셨다. 정적 사이트라 브라우저에 키를 못 넣고,
  **숫자를 LLM이 만들면 `figures.py` 로 지켜온 게 통째로 무너지기** 때문이다. 2단계(LLM 다듬기)로
  가더라도 숫자는 규칙이 확정하고 LLM에게는 말 짓는 일만 시킨다.
  - **⚠️ 기준선 없는 절대 숫자는 오해다. 이게 이번 건의 핵심이다.**
    처음에 "52주 고가에서 32% 빠진 자리"라고 썼다. 숫자는 맞는데 나쁘게 읽힌다. 세어 보니
    **개별 종목 2,666개의 중앙값이 -44%** 였다. -32%는 평균보다 **나은** 자리였다.
    → **"고가 대비 O%"를 쓸 일이 있으면 중앙값을 같이 적어야 한다.** 블로그·DM도 같다.
  - **모집단을 문장에 박는다.** 12318이 같은 값을 다시 세다 -27% 를 얻었는데,
    `screener_results.json`(조건 통과 200개)으로 쟀기 때문이었다. 스크리너 생존자는 고점 근처에
    있는 게 당연하다. **숫자가 틀린 게 아니라 무엇을 세는지가 달랐다** — 오늘만 네 번째다.
    그래서 화면 문장이 "개별 종목 2,666개의 중앙값이 -44%라" 하고 대상을 밝힌다.
  - **적용 범위**: 조건이 계산된 건 772개(28%)뿐이다. 나머지는 가격·52주 위치·거래량·테마로 얇게
    답한다. **빈손으로 돌려보내지 않는다** — 어제 코데즈컴바인 제보의 교훈이다. 얇은 답에도 기준선을 붙인다.
    ETF·ETN은 개별 종목 기준선과 견주지 않는다(다른 것끼리 견주면 숫자가 맞아도 뜻이 틀린다).
  - **해석·전망 잠금장치** `SUMMARY_BANNED` 25개. 걸리면 그 문장만 빼고 나머지는 보여준다.
    한두 글자로 막으면 종목 이름에 헛걸린다 — `중단기회사채`·`코스피양매도5%OTM`·**좋은사람들(033340)**.
    판정하는 꼴만 막는다. 화면에 **"AI"라는 낱말은 안 쓴다**(12318 제안). 제목은 `🧭 지금 어느 자리인가`.
  - 고정 꼬리 `종목 추천이 아닙니다. 조건에 걸렸는지만 계산합니다.` 를 요약 블록 **안에** 뒀다.
    캡처돼 돌아다닐 때 같이 가야 한다(12318 제안).
  - 새로 받는 파일: `data/52w_cache.json` · `data/ath_cache.json` (모달 처음 열 때만, 합쳐 230KB)

- **12317** — 🐛 **테마별 탭 검색이 전체의 21%만 보고 있었다.** 이용자 제보로 알았다.
  "코데즈컴바인이 테마별에서 안 찾히는데 관심 탭에서는 추가된다"는 문의였다. **테마가 없어서가
  아니었다** — 그 종목은 남북경협(121)·패션의류(32)·마스크(285)·일본불매(421) 네 개에 들어 있다.
  - **원인**: `renderContent()`(`index.html`)가 검색어를 `getAllThemes()` 가 돌려준 테마의 소속
    종목에만 걸었다. 그 목록은 강세 전환 20 + 네이버 강세 상위 20 + 업종 상위 10 + 직접 적은 것뿐이라,
    **네이버 테마 267개 중 37개만 오른다.** 남북경협이 강세가 아닌 날에는 그 안의 종목이 통째로 사라졌다.
    오늘 기준 **4,300종목 중 906개(21%)만 찾혔다.** 현대차·기아도 안 나왔다.
  - **검색창이 둘인데 보는 데가 달랐다.** 머리글 🔍(`:3456`)와 관심 탭(`:5224`)은 `MARKET.stocks`
    전체를 본다. 테마별 탭 것만 21% 를 봤다. 생김새가 같으니 이용자가 헷갈린 게 당연하다.
  - **고침**: 검색어를 쳤을 때만 테마 밖 종목도 `🔎 테마 밖` 묶음으로 같이 보여준다. **탭 목록은
    그대로 둔다** — 267개를 다 올리면 고르기가 더 나빠진다. 카드 그리는 몫을 `themeCardHtml()` 로
    뽑아 테마 안팎이 같은 모양으로 나오게 했다.
  - ETF·ETN 은 빼지 않고 **뒤로 보냈다.** "삼성전자"로 찾으면 열여덟 중 열여섯이 ETF·ETN 이라,
    순서를 안 잡으면 30개 상한에 잘려 정작 삼성전자가 밀린다. 배지로 알아보게 했다.
  - 진짜 시세 4,300종목으로 돌려 확인했다: `047770`·`코데즈`·`005380` 전부 나온다. 빈 검색어면
    테마 밖 묶음은 안 뜬다. 한 글자를 치면 1,975개가 걸리는데 30개에서 끊고 "더 좁혀서" 안내를 붙인다.
  - ~~`theme_members_cache.json` 이 멈춰 있다~~ → **헛짚었다. 이레마다 정확히 갱신되고 있다.**
    07-29 · 08-05 · 08-12 · 08-19 · 08-26 · 09-02 · 09-09 · 09-16 — 한 번도 안 빠졌다.
    `theme_forecast.py:69` 가 `cache_age >= 7` 일 때만 새로 받게 돼 있다. 다음은 09-23 이다.
    나흘 지난 것을 보고 멈췄다고 적었다. **다른 세션 시간을 쓸 뻔했다.**
  - 다만 **진짜로 걸리는 것이 하나 있다**(급하지 않다). `theme_forecast.py:411` 이 `themes[:80]`
    만 새로 받는다. 캐시에는 테마가 267개인데 이레마다 갱신되는 건 그때 상위 80개뿐이고,
    **나머지 187개의 멤버 목록은 처음 받은 날 그대로다** — 7월 것도 있다. 갯수가 267에서
    한 번도 안 는 게 그 증거다. 오래된 테마에 새로 상장한 종목이 빠져 있을 수 있다.
    이번 코데즈컴바인 건과는 무관하다(남북경협에 제대로 들어 있었다).

- **12317** — 추세 조건의 **60일선 판정을 그림자로 같이 쌓기 시작했다.** 판정은 그대로 50 이다.
  사장님이 "한국은 20·60 을 쓰는데 50 이 맞냐"고 물으셔서 **매도 규칙의 트레일링**은 재봤다.
  차이가 없었다 — 미너비니 923건에서 승률 16.0%(50) 대 15.7%(60), 최악도 같았다. 그 규칙은
  +50% 넘게 벌었을 때만 쓰이는데 그런 거래가 4.1% 뿐이라 **표본이 답할 힘이 없었다.**
  진짜 물음인 **추세 조건의 50일선**은 그때 못 쟀다. 보관함에 50 기준 통과 여부만 박혀 있어
  되돌려 셀 수가 없다. 그래서 오늘부터 쌓는다.
  - `screener.py` — `ma60` 을 같이 구하고 `trend_template` 에 `_shadow_price_above_ma60` ·
    `_shadow_ma60_above_ma150` 을 넣는다. **이름 앞에 `_` 를 붙였고 `TT_BOOL_KEYS` 에는 안 넣었다.**
    `trend_checks` 는 여덟을 이름으로 꼽는 목록이라 점수·통과 수·화면의 "추세 N/8" 은 안 바뀐다.
    (합성 데이터로 확인: 그림자를 넣기 전후로 `tt_passed_count` 와 접은 자리 `tt` 가 같다)
  - **`data/screener_conditions_history/` 가 새로 생긴다** — 하루 27KB · 772종목.
    `screener_results_history` 는 상위 100개만 담는데 **그 100개를 고른 잣대가 50일선이라**,
    60 이었으면 뽑혔을 종목이 애초에 안 들어온다. 그것만 보면 50 쪽으로 기운 표본을 본다.
    그래서 전 종목을 따로 남긴다. `.gitignore` 에 없으니 커밋된다(1년 6MB, 기존 보관함의 1/4).
  - `scripts/ma_5060_compare.py` — 쌓인 것으로 셀 수 있는 것만 센다. 판정이 갈린 비율은
    며칠이면 읽히고, **수익률 비교는 2026-12-27 쯤**이라야 표본이 찬다(거래일 40일 + 뒤 30일).
    그 전에 열어 보면 남은 날수만 찍는다. 처음에 "11월쯤"이라고 말씀드렸는데 **틀렸다.**
    40일이 쌓이는 날만 세고 그 40일치가 저마다 30일을 채우는 걸 안 세서 보름쯤 일렀다.
- **12317** — 낱말 검사기가 `키트/posts/_발행본_지표용어_색인.md` 를 헛걸었다. 그 색인은
  "21일선을 20일선으로 바꿨다"를 **적어 두는 곳**이라 옛 이름이 나오는 게 정상이다.
  `작업일지.md` 와 같은 부류라 `SKIP` 에 넣었다. 검사기는 다시 조용하다.

## 2026-09-19

- **12318** — ⚠️ **키트가 두 벌이었다. 그 탓에 낡은 숫자가 실제 답글로 나갔다.**
      A  `C:\블로그자동화-실전키트`                          18커밋 · 진행상황 612줄 · 원고·카드 전부
      B  `richman\블로그자동화-실전키트`                      6커밋 · 진행상황 240줄(09-17 에서 멈춤)
  **서로 다른 git 저장소**라 커밋이 서로 안 보였다. 12317 이 "12318 커밋이 없다"고 한 것도
  진짜였다. **사장님이 원고·규칙의 정본을 A 로 정하셨다.**
  - **실제 피해**: 12316 이 B 의 `CLAUDE.md` 를 보고 쓰레드 답글에 `907건·82.9%·13.1%`
    를 썼다. 정본에는 `918건·82.4%·13.6%` 로 고쳐져 있었고 "907 은 09-14 값" 주석까지 있었다.
    차이는 건수 +11 · 비율 ±0.5%p 라 **결론(-5% 는 대부분 손절당한다)은 안 뒤집혔다.**
    답글 수정 여부는 사장님 판단으로 둔다.
  - B 에만 있던 넷은 A 로 가져왔다(A 의 `ebbf210`): `.gitattributes` ·
    `기준문서/말투.md`(**A 쪽은 안 채운 템플릿이었다**, 85→183줄) ·
    `posts/260917_실시간아닌이유.md` · `CLAUDE.md` 의 `(8) 운영 방식` 절.
    B 의 손절선 숫자(907건)는 옛값이라 안 가져왔다.
  - ⛔ **B 를 통째로 버리면 안 된다.** `댓글/상태.json`·`대상.json`·`큐데이터.json` 과
    `scripts/댓글수집.mjs`·`블로그찾기.mjs`·`큐만들기.mjs` 가 **B 에만 있다.**
    처음에 "이 폴더 쓰지 않음"으로 표시했다가 `git add` 경고에서 `상태.json` 을 보고
    되돌렸다. 그대로 갔으면 다음 사람이 A 에 `댓글/` 을 새로 만들어 **상태가 두 벌이 되고
    같은 사람에게 댓글이 두 번 갔을 것이다.** B 에 표지를 세워 뒀다(`58cb0cb`).
  → **어디를 보나**: 원고·규칙은 **A**, 댓글 자동화는 **B**. B 의 `CLAUDE.md` 는 고치지 않는다.

- **12318** — **숫자 하나를 고치면 그것으로 낸 값까지 전부 다시 낸다.**
  태성 최고가를 94,200 → 94,000 으로 고쳤는데, 그것으로 계산한 값을 안 고쳤다.
      8.2배 / 8.16배 → **8.15배**,  +716% → **+714.6%**,  21.8배=+2,081% → **21.76배=+2,076.4%**
  12317 에게 +2,081% 를 잘못 넘긴 것도 여기서 베꼈기 때문이다. 발행글은 배수만 쓰고
  퍼센트가 없어 무사했다. 키트 `CLAUDE.md` 에 이 규칙을 적어 뒀다.

- **12318** — **인수인계 구멍 셋을 막았다.** 작업일지는 잘 돌았는데(오늘 40건, 서로
  세 번 교정) **일지가 닿지 않는 곳**이 있었다.
  1. **`블로그자동화-실전키트` 가 9/16 이후 커밋이 없었다.** 글쓰기 규칙·태성 숫자
     정정이 전부 커밋 밖에 있어서, 세션 브리핑(richman 커밋만 본다)에 안 잡혔다.
     12317 이 옛 CLAUDE.md 를 봤으면 또 "200% → 400%" 를 썼을 것이다. 두 건으로 나눠 커밋했다.
     ⚠️ 그 저장소는 **원격이 없다.** 이력은 로컬에만 있다.
  2. **`threads-dm` 은 git 자체가 없었다.** DM 을 실제로 쏘는 코드인데 되돌릴 수단이
     `.bak` 수동 복사뿐이었다. 그 자리에서 `git init` 했다(2bde12a · 스크립트 16개).
     옮기지 않은 이유: `cycle.sh` 가 자기 폴더 기준으로 `sent.log`·`my_comments.json` 을
     읽고 12316 스크립트도 같은 경로를 쓴다.
     **남의 아이디·블로그 주소가 든 파일은 .gitignore 로 처음부터 뺐다**
     (`sent.log`·`*.json`·`pick*.txt`·`rest.txt`·`shadow_reply.log`). 원격이 붙는 순간
     공개되기 때문이다. 하나씩 `git check-ignore` 로 확인했다.
  3. 이 일지에 **아침에 "아직 안 고쳤다"고 적고 저녁에 고친 항목**이 어긋난 채 있었다.
     새 항목만 위에 추가하면 옛 줄이 살아남는다. ✅ 표시로 어디를 보라고 이어 뒀다.

- **12318** — 윈도우에서 파일을 고칠 때 **줄바꿈이 통째로 바뀌는 사고를 두 번 냈다.**
  - 파이썬으로 `io.open(f,'w')` 하면 LF 파일이 CRLF 로 바뀐다. `글쓰기-표준.md` 가
    한 섹션만 고쳤는데 **189줄 바뀐 것**으로 나왔다. 그대로 커밋했으면 나중에
    `git diff` 로 뭐가 바뀐 건지 못 찾는다. 내용은 안 건드리고 LF 로 되돌려 커밋했다.
  - 이 저장소의 `작업일지.md` 는 반대로 **CRLF** 다. `newline=''` 로 읽으면 앵커가
    `

` 이라 `
` 으로 짠 치환이 전부 빗나간다. `newline` 을 주지 않고 열면
    읽을 때 `
` 으로 바뀌고 쓸 때 원래대로 돌아간다.
  → **남의 파일을 고칠 때는 줄바꿈부터 확인한다.** 섞어 쓰는 저장소다.
  - `.gitignore` 는 **같은 줄 뒤에 주석을 못 단다.** `!rb.js  # 설명` 이라고 썼더니
    패턴이 `rb.js  # 설명` 이 되어 파일이 안 들어갔다. 주석은 윗줄에 쓴다.
  - 이 윈도우는 `core.autocrlf=true` 다. 그냥 두면 체크아웃될 때 `.sh` 가 CRLF 가
    되어 **bash 가 깨진다.** `threads-dm/.gitattributes` 에 `eol=lf` 를 박았다.

- **12316** — **다른 세션이 손대는 파일은 줄 번호로 가리키면 안 된다.** 오늘
  `content/posts.yml` 의 옛 CTA 위치를 줄 번호로 12317 에 넘겼는데, 그 사이
  12317 이 글을 끼워 넣어 파일이 밀렸다. 내가 "대기열 두 편이 남았다"고
  사장님께 보고한 것도 그래서 틀렸다(실제로는 전부 발행된 글이고 대기열엔 없다).
  grep 과 확인을 다른 호출로 나눠 한 사이에 파일이 바뀐 것이다.
  → **`kind`·`slug` 같은 안 변하는 값과 문구로 가리킨다.** 줄 번호를 줄 거면
     같은 호출 안에서 읽고 바로 넘긴다.

- **12318** — ⚠️ **같은 이름(`entry_days`)을 쓰는 두 파일이 다른 걸 세고 있었다.**
  발행한 블로그 글에 틀린 숫자가 나갔고, **12317 이 독립으로 세어보다 잡았다.**
  - `stop_sensitivity.json` entry_days = **57** ← 평가 **전** 픽 전체의 날짜
  - `backtest_stats.json` period.entry_days = **54** ← 실제로 수익률이 계산된 918건의 날짜
  - 표의 건수(918)는 평가된 것만 센 값인데 기간은 전체에서 냈다. **둘이 다른 대상이었다.**
    주석에는 "backtest.py 와 같은 모양으로 맞춘다"고 적혀 있었다. 모양만 같았다.
  → `scripts/stop_sensitivity.py` 를 고쳤다(a65f8518). 평가에 쓰인 픽(`used`)으로
    기간과 종목 수를 낸다. **건수와 기간은 같은 대상에서 나와야 한다.**
  → 발행글도 고쳤다(logNo 224417295382 · 본문 한 줄 + 7번 카드 이미지 교체).
  → `CLAUDE.md` 에 "entry_days 는 평가된 픽 기준. 옛 파일은 backtest_stats 와 대조" 를 넣었다.
  ⏳ **코드는 고쳤지만 `data/stop_sensitivity.json` 은 아직 57 이다.** 이 파일은
    Momentum Screener 가 끝나야 다시 만들어진다(`backtest.yml` 은 workflow_run 으로 물려 있다).
    **그때까지 이 파일에서 기간을 인용하면 안 된다.** 오늘 인용할 일이 있으면
    `backtest_stats.json` 의 `categories.minervini_strong.30d.period.entry_days`(54)를 쓴다.
    다음 실행 뒤 두 파일의 entry_days 가 같아졌는지 확인할 것. (12317 이 짚었다)
  ⭐ **교훈**: 값을 두 번 봐도 정의가 어긋나 있으면 못 잡는다. 같은 숫자를 **다른 세션이
    독립적으로 세어본 것**이 유일한 발견 경로였다. 숫자를 넘길 때는 값만 주지 말고
    **어느 파일 어느 키에서 나왔는지** 같이 준다.

- **12318** — `threads-dm/cycle.sh` 링크 요청 경로를 열었다. **다만 그림자로만 돈다.**
  `_ours()` 가 버리던 '내 댓글에 달린 답글'을 `R` 로 표시해 넘기고, 기존
  `link_request` 분류기를 그대로 태운다(두 번째 규칙을 만들지 않았다).
  - `SHADOW_REPLY=1` 인 동안은 **보내지 않고** `shadow_reply.log` 에 시각·아이디·문장만 적는다.
    이 경로는 원래 DM 이 안 나가던 사람에게 처음 열리는 문이고 DM 은 못 되돌린다.
    사장님이 며칠 보시고 `SHADOW_REPLY=0` 으로 바꾸면 켜진다.
  - 퍼머링크가 없어 내용을 못 읽으면 **안 보낸다**(`REPLY_NO_PERMA`).
  - `sent.log` 중복 대조는 이 경로에도 그대로 걸린다. 확인했다. DM 창의 `stage2.kr`
    검사까지 이중이다.
  - `shadow_reply.log` 는 로컬 전용이다. **공개 저장소에는 아이디도 본문도 남기지 않는다.**

- **12318** — **쓰레드 DM 경로는 `aria-label` 변경에 안 걸린다. 실제로 확인했다.**
  12316 이 넘긴 건을 받아 라이브 DOM 을 봤다.
  - `/activity/replies` : `[aria-label="답글"]` **0개** → 12316 말이 맞다.
    `svg > title` 에 답글 26 · 리포스트 48 · 공유하기 48 · 좋아요 38 로 들어가 있다.
    **개수가 서로 다르다.** 순서로 세면 밀리는 이유가 이거다.
  - **DM 쪽(`threads-dm/cycle.sh`, `recheck-held.sh`)은 무사하다.** 라벨이 아니라
    `innerText.trim() === '메시지 보내기'` 로 찾는다. @tommy21c 프로필에서 **정확히 1개**
    잡혔고(`DIV[role=button]`), 팔로우 판정 정규식도 그대로 걸린다.
    DM 창 입력칸도 `[contenteditable="true"][role="textbox"]` 라 라벨과 무관하다.
  - `aria-label` 을 읽는 건 일회용 디버그 스크립트 셋뿐이다(`chk4.js`·`find게시.js`·`vb.js`).
    `vb.js` 는 이미 `svg title` 을 대체로 읽고 있다. 손댈 것 없다.

- **12318** — ⚠️ **링크 요청이 12316 → 12318 로 안 넘어오는 구멍이 있다.**
  `cycle.sh:147-154` 의 `_ours()` 가 **내 댓글에 달린 답글을 DM 대상에서 뺀다.**
  `my_comments.json` 에 등록된 댓글로 시작하는 문맥이면 제외한다.
  원래 의도는 맞다 — 그게 없으면 내 댓글에 답한 사람에게 "댓글 주셔서 링크
  보내드려요" 가 나가서 스팸이 된다.
  그런데 12316 이 **댓글에 링크를 안 넣기로** 했으므로, 링크를 원하는 사람은
  바로 그 댓글에 답글로 요청하게 된다. **지금 구조면 그 사람이 통째로 걸러진다.**
  → 고치려면 "내 댓글에 달린 답글"이라도 **링크 요청 키워드가 있으면 통과**시켜야 한다.
    DM 경로를 건드리는 일이라 사장님 승인 아래 하겠다.
  ✅ **같은 날 저녁에 사장님 승인받고 고쳤다. 위쪽 '링크 요청 경로를 열었다' 항목을 보라.**
    다만 `SHADOW_REPLY=1` 인 동안은 적기만 하고 보내지 않는다.

- **12318** — 블로그 발행: `주식 매도 타이밍, 저는 8배를 놓치고 나서 선을 그었습니다`
  (logNo 224417295382 · 본문 1,567자 · 이미지 8장). 쓰레드 대기열용 참고.
  태성 실매매가를 사장님께 받아 네이버 일봉으로 교차검증했다 —
  4,319 → 11,540(+167%) → 최고 94,000(판 값의 8.15배) → 2026-09-18 45,250(-51.9%).
  **CLAUDE.md 에 있던 "200% 먹고 400% 더 갔다"는 틀린 숫자였다.** 바로잡았다.

- **12318** — **에디터 자동입력에 대해 09-16 에 적은 진단이 틀렸다. 정정했다.**
  "창이 가려지면 입력이 안 먹는다"고 적었는데 아니다. 타이핑은 `visibilityState`
  와 무관하게 먹고, **좌표 클릭만** 맨 앞 창으로 간다(tabId 를 줘도 무시).
  09-16 에 본문이 날아간 진짜 원인은 **글감·라이브러리 패널이 입력을 가로챈 것**이다.
  전문은 `블로그자동화-실전키트/진행상황.md` 맨 뒤. 쓰레드 쪽도
  "hidden 이면 멈춘다"고 적어 두셨는데, 그건 쓰레드 화면에서 다시 재보는 게 좋겠다.

- **12316** — **쓰레드가 액션 아이콘의 `aria-label` 을 없앴다.** 답글 버튼을
  `[aria-label="답글"]` 로 찾으면 **0개**가 나온다. 라벨은 `svg > title` 안으로
  들어갔다(`좋아요 · 답글 · 리포스트 · 공유하기`). 쓰레드 DOM 을 건드리는 쪽은
  전부 같이 깨진다. **DM 쪽도 같은 방식으로 라벨을 찾고 있으면 확인할 것.**
  ✅ **확인했다(12318). DM 은 안 걸린다** — 라벨이 아니라 `'메시지 보내기'` 텍스트로 찾는다.
    위쪽 '쓰레드 DM 경로는 aria-label 변경에 안 걸린다' 항목에 실측을 적어 뒀다.
  - 왼쪽에서 두 번째(순서)로 잡으면 안 된다. 좋아요 아이콘이 덜 그려진 순간에
    재면 한 칸 밀려 **리포스트가 눌린다.** 오늘 실제로 눌렀다(메뉴만 뜨고 안 걸림).
  - 이틀 동안 이걸 **계정 제한**으로 잘못 짚고 있었다. 사장님이 직접 달아 보시고
    된다고 하셔서 알았다. 아이콘이 0개면 제한을 의심하기 전에 선택자를 먼저 본다.

- **12316** — 쓰레드 작성칸에 `execCommand('insertText')` 로 글을 넣은 **직후**
  `innerText` 를 읽으면 빈 값(`
`)이 나온다. 안 들어간 게 아니라 늦게 반영된다.
  다시 넣으면 두 번 들어간다. **0.4초 기다렸다 읽는다.**

- **12316** — 사장님 지시로 규칙 하나 추가. **등록을 감지해도 곧바로 다음 글로
  가지 않고 3초 쉬었다 넘어간다.** 바로 넘기면 등록이 화면에 반영되기 전에
  페이지가 바뀐다.

- **12316** — 쓰레드 검색어 중 **쓰면 안 되는 것**을 찾았다.
  `오늘 손절했다`(야구·굿즈·잡담), `주식 물렸어`(언쟁 글만), `주린이`(술집 창업까지).
  쓸 만한 것 — `매매일지` · `주식 복기` · `주식 손절` · `주식 멘탈` · `투자일지`.

- **12316** — 오늘 몫 끝. 블로그 10건 + 쓰레드 10건, 전부 처음 만난 사람이다.
  쓰레드 계정 열과 거른 계정 열둘을 `댓글/상태.json` 에 적었다.
  이 파일은 `.gitignore` 라 저장소엔 안 올라간다. **DM 보내기 전에 오늘 댓글 단
  사람과 겹치는지 보려면 그 파일을 봐야 한다.**

- **12316** — 내 인수인계서를 `블로그자동화-실전키트/댓글/작업요령.md` 에 만들었다.
  겪은 것만 적는다. 크롬으로 네이버·쓰레드를 만지는 세션은 읽고 시작하면
  같은 데서 두 번 안 막힌다(포커스·한글 입력·서로이웃 팝업·선택자).

## 2026-09-17

- **12318** — **블로그 본문 숫자 표기 기준이 바뀌었다.** 데이터인 숫자는
  아라비아 숫자로 쓴다(`36일 중 6일`, `1개였던 날이 16일`, `21건`, `13건뿐`).
  말의 리듬으로 쓰는 수는 한글로 둔다(`조건을 세 개`, `두 번 넘게`, `세 번째가`).
  가르는 기준은 "이 숫자가 이 문단의 근거인가". 사장님 지적으로 정해졌다.
  → `블로그자동화-실전키트/기준문서/글쓰기-표준.md` 에 넣었다.
    **카드 이미지도 같은 규칙이다.** 본문만 고치면 카드와 표기가 어긋난다.
    쓰레드 문안도 이 기준으로 맞추면 된다.

- **12318** — **봇은 링크 요청에만 답글을 단다**(사장님 지시). 인사도 이제 안 단다.
  `content/social.yml` 에 `engage.auto_reply_rules: ["link_request"]` 를 넣었다.
  여기 적힌 규칙만 답글을 단다. 목록을 통째로 지우면 예전처럼 전부 답한다.
  - 그래서 `thanks`("감사합니다" → 인사)와 `default`("읽어주셔서 감사합니다!")가 꺼졌다.
    문구는 규칙에 그대로 두었다. 되살리려면 이름만 목록에 추가하면 된다.
  - `link_request` 만 남긴 이유: 답이 아니라 달라고 한 것을 건네주는 일이고,
    쓰레드는 이 답글이 나가야 사장님께 "직접 DM" 알림이 간다.
  - 인스타 경로도 같은 `matcher.match` 를 타므로 DM 까지 같이 막힌다
    (link_request 의 dm 은 그대로 나간다).
  → **봇 답글 수가 크게 준다.** 지금까지 댓글 대부분에 답이 나갔는데 이제 링크 요청만이다.
    쓰레드 지표를 볼 때 이걸 감안해야 한다.

- **12318** — **질문 댓글에는 봇이 답글을 달지 않는다**(사장님 지시).
  오늘 질문한 댓글에 `default` 의 "댓글 감사해요!" 가 나갔고, 그 바람에
  사장님이 답할 것을 놓치셨다. 봇이 덮으면 사람이 못 본다.
  - `content/social.yml` 에 `engage.no_generic_reply_on_question: true`
  - 질문 판정: 물음표 · `~나요/까요/은가요/인가요/는지/을까/ㄹ까` 로 끝남 · `궁금/여쭤/질문`
  - **허용 목록 방식이다.** `answers_question: true` 인 규칙만 질문에 답한다.
    지금은 `link_request` 하나뿐 — 그건 답이 아니라 달라고 한 것을 건네주는 일이고,
    쓰레드는 이 답글이 있어야 운영자에게 "직접 DM" 알림이 간다.
  - 처음엔 인사(thanks·default)만 막았는데 **"손절은 어떻게 잡으시나요?" 가
    `how_it_works` 의 '어떻게' 에 걸려** 종목 고르는 기준을 읊었다. 질문 규칙의
    낱말은 넓을 수밖에 없어서 막을 것을 고르는 방식으로는 계속 샌다. 그래서 뒤집었다.
  - 답 안 단 질문은 텔레그램으로 모아 알린다(`social_engage.finish`). 거르기에
    걸린 시비 댓글은 이 목록에 안 넣는다. 아이디와 글 주소만 남기고 본문은 안 남긴다.
  → **알림·기준·가격 질문도 이제 봇이 답하지 않는다.** 그 문구들은 규칙에 그대로
    남아 있으니, 되살리려면 해당 규칙에 `answers_question: true` 만 붙이면 된다.

- **12318** — `scripts/social/matcher.py` 의 거르기도 cycle.sh 와 같은 오탐이 있어 같이 고쳤다.
  `skip_keywords` 를 공백 지운 문자열에서도 보고 있어서 `회사 기준`→`사기` 가 됐다.
  이제 거르기는 원문에서만 보고, 3자 이하 낱말은 공백 없이, 그보다 긴 말토막
  (`디엠주세요`)은 사이 공백을 허용한다. `신고` 는 `_SKIP_EXCEPT` 로 `신고(?!가)`.

- **12318** — `threads-dm/cycle.sh` 링크요청 판정 오탐을 **고쳤다**(사장님 지시).
  **거르기(SKIP)를 공백 지운 문자열이 아니라 원문에서 정규식으로 본다.**
      SKIP_RE = re.compile(r'신고(?!가)|사기|리딩|손해|환불|고소|광고|디엠[ ]*주세요|팔로우[ ]*맞팔')
  공백을 살리니 `회사 기준`·`조사 기간`·`검사 기록`이 더는 '사기'로 안 걸린다.
  `신고(?!가)`로 계정 주제인 `신고가`는 통과하고 `신고합니다`·`신고할게요`는 그대로 걸린다.
  `리딩방`은 `리딩`이 덮어서 뺐다. 백업 `.bak/cycle.sh.20260917-신고가오탐수정전`.
  ⚠️ **sent.log 에는 아이디만 남고 댓글 본문이 없다**(공개 저장소 원칙). 그래서 과거
  NOT_LINK_REQUEST 를 되돌려 돌려볼 수 없었고, 대신 합성 27건으로 세 구간을 확인했다.
  본문을 로그에 남기는 건 그 원칙과 부딪혀서 하지 않았다.

  12317 이 대기열 문안을 문장형으로 바꾼 것 자체는 매칭을 안 깬다 —
  공백·구두점을 지우고 보기 때문에 문장 안에 섞인 `노트` 는 정상적으로 잡힌다.
      yes  "저는 바로 안 들어가요. 노트 주세요"
      yes  "매일 보긴 힘들더라구요. 노트 받아볼 수 있을까요?"

- **12318** — **네이버 에디터 자동입력에서 본문이 두 번 날아갔다.** 에디터를
  스크립트로 만지는 세션은 보기 바란다. 넷 다 실측이다.
  1. **이모지를 문장 앞에 치면 뒤가 통째로 잘린다.** `💬 52주…` → `💬` 만 남는다.
     게다가 이모지만 남은 문단은 그 뒤에 커서를 놔도 입력이 안 먹는다. 본문에 이모지를 쓰지 않는다.
  2. **크롬 창이 OS 포커스를 잃으면(`visibilityState==='hidden'`) 입력이 전부 무시된다.**
     type 도 keyboard 도 success 를 돌려주면서 아무 일도 안 한다. 좌표 클릭은 그 사이 다른 창에 간다.
  3. **`triple_click` 금지.** 문단 하나가 아니라 19개 문단을 잡아서 타이핑 한 번에 날렸다. `Ctrl+Z` 도 안 들었다.
  4. **문단 첫 칸에서 Backspace 연타 금지.** 위쪽 이미지와 문단 7줄이 지워졌다.
  → 안전 절차와 `focus_editor.ps1` 요령은 `블로그자동화-실전키트/진행상황.md` 맨 뒤에 적었다.
    줄 교체는 `left_click` → `End,Shift+Home` → 타이핑이 제일 안전했다.

- **12318** — 블로그 발행: `52주 신고가, 걸러보면 하루 한 종목입니다` (logNo 224414301181).
  본문 1,428자 · 이미지 8장. 쓰레드 대기열에 넣을 때 참고 바란다.

- **12316** — **크론 배분 모형: 1슬롯짜리는 거의 100% 돈다.** 12317 이 제안한
  "저장소 단위냐 워크플로 단위냐" 를 가르려고 손 안 댄 워크플로를 세어봤다.
  9/8~9/16 커밋 기준이다.
  ```
  exit-signals (1슬롯)  하루 1회 안정   ← 9/14 만 4회(수동 추정)
  screener     (1슬롯)  하루 1회 안정
  fetch-prices (구 */5) 하루 2~4회      ← 예정은 약 96회
  user-alerts  (구 */5+16) 하루 5~7회   ← 예정은 약 112회
  ```
  **저장소 단위 몫 모형이면 1슬롯짜리도 같이 떨어져야 하는데 안 떨어진다.**
  워크플로마다 따로 조이는 쪽에 가깝고, 슬롯을 많이 요구할수록 더 깎인다.
  → 그래서 12317 이 제안한 "남은 워크플로가 늘어나는지" 검사는 갈리지 않을 수 있다.
    게다가 **fetch-prices 는 내가 어제 건드려서 대조군이 아니다.**
    대조군으로 쓸 수 있는 건 exit-signals · screener · metadata · us-screener 다.

  **기준선(변경 전, 9/8~9/16 커밋 수)** — 며칠 뒤 이 숫자와 견준다.
  ```
  fetch-prices  22회 / 7거래일 = 3.1회
  exit-signals  11회 / 7거래일
  screener      11회 / 7거래일
  ```

- **12316** — **5분 크론이 실제로는 안 돌고 있었다.** `*/5 0-7` 로 걸어 둔
  `fetch-prices` 와 `user-alerts` 가 9월 내내 하루 2~4회만 돌았다.
  고빈도 예약은 깃허브가 대부분 버린다. **목표가 알림이 19:11·22:45·23:57 처럼
  밤에 나가고 있었다** — 장 끝나고 여섯 시간 뒤다. 시각 고정으로 바꿨다.
  정규장이 15:30 에 끝나는데 마감 확정을 16:33 에 받고 있어 **15:35** 로 당겼고,
  2026-09-14 부터 시간외가 16~20시 실시간이라 **20:05** 를 새로 넣었다.
  → 크론을 또 건드릴 일이 있으면 12317 의 Cloudflare Worker `SCHEDULE` 도 같이 볼 것.
    (이번 건은 발행이 아니라 시세·알림이라 겹치지 않았다)

- **12316** — 사이트 갱신 시각 배지를 누르면 갱신 일정이 뜨게 했다.
  독자가 "시세는 언제 업데이트?"를 실제로 물어봤다. 고치다 `fmtRelativeTime` 이
  **초 없는 시각을 못 읽고, UTC 에 무조건 +09:00 을 붙이던 것**을 찾았다
  (`us_results.json` 이 UTC 라 미국 탭이 아홉 시간 어긋날 상황이었다).
  오닐·미국 탭은 데이터 도착 후 배지 갱신 호출이 빠져 있어 늘 비어 있었다.

- **12316** — 댓글 자동화를 돌렸다. 하루에 65명(블로그 37·쓰레드 28), 답글 15건,
  서로이웃 6건 신청·2건 성사. 알아낸 것 둘을 키트 `진행상황.md` 에 적어뒀다.
  ① 네이버는 `blog.naver.com/PostView.naver?blogId=..&logNo=..` 로 들어가야
     댓글창에 손이 닿는다(일반 주소는 iframe). 쓰레드는 반대로 글 주소가 피드로
     튕겨서 피드에서 답글 버튼을 찾아야 하고, **한글은 키 입력이 안 돼서**
     `execCommand('insertText')` + `Shift+Enter` 로 넣는다.
  ② 서로이웃 팝업이 첫 [다음] 에서 닫히면 **이미 신청이 걸려 있는 것**이다.
     글 페이지의 "이웃추가" 글자는 대기 상태를 반영하지 않으니 판단 근거로 쓰면 안 된다.

- **12316** — 미안합니다. 커밋 `66e8be6b` 에 12317 의 `content/posts.yml` 551줄이
  섞여 들어갔습니다. `git add -A` 를 쓴 탓입니다. 앞으로 고친 파일만 담겠습니다.

- **12317** — 자동 글 끝맺음도 질문 + 링크 안내로 바꿨다. 어제 대기열 31편만
  바꾸고 **자동 글이 옛 형태로 남아 있었다.** 자동 글이 더 자주 나가므로
  이쪽이 더 크다. `content/social.yml` 의 `post.cta` 가 근원이었다.
  · 자동 글은 매일 같은 틀이라 질문을 하나로 못 박으면 그 자체가 반복이 된다.
    여섯 개를 두고 **날짜로 돌려 쓴다**(`post.cta_questions`).
  · 시황 글(`kr_close`·`us_brief`)은 `no_cta` 라 원래 안 붙는다. 자체 질문으로
    끝난다("오늘 어떤 종목 보셨어요?").
  → 쓰레드에서 어제 나간 글을 직접 보고 찾았다. 파일만 보면 안 보인다.

- **12317(해결)** — 쓰레드 고정글의 `21일선` 을 사장님이 바꾸셨다. 쓰레드에서
  직접 확인했다 — 21일선 0건, 20일선·교대근무·918건·새 매도 기준 세 줄 전부
  들어가 있다. 이제 고정글·화면·알림이 같은 말을 한다.

- **12317** — 낱말 검사기에 키트 원고를 넣고(`C:\블로그자동화-실전키트`의
  `posts/`·`기준문서/`·`CLAUDE.md`), **규칙 둘을 뺐다.**
  · 뺀 것: `5분마다 갱신`, `ath_cache`. 이름이 바뀐 게 아니라 한 번 고친
    문구라 맥락에 따라 맞기도 하다. 실제로 「왜 실시간이 아닌가」 글이
    **"5분마다라고 적어놨는데 틀렸다"** 를 두 번 인용하는데 검사기가 그걸
    고칠 자리로 잡았다. **고친 자취를 고치라고 하는 셈이다.**
  · 남긴 것: `21일선`·`MA21`·`21일 평균선`. 어디에 나오든 무조건 틀린 것들.
  · `_폐기_` 원고와 원고 앞머리(`---` 사이)는 뺀다. 둘 다 "전에는 이렇게
    썼다"가 적히는 자리다.
  → **헛걸리는 경고는 결국 안 읽힌다.** 확실한 것만 남기는 게 낫다.

- **12317** — **낱말이 낡는 것도 검사로 잡는다** (`scripts/stale_terms.py`).
  숫자는 `figures.py` 가 발행 직전에 맞추는데 낱말은 못 잡는다. 값이 아니라
  이름이라서다. 오늘까지 세 번 있었다.
  · 고정글이 `21일선` 인 채였다 (화면은 20일선으로 계산)
  · 12318 의 블로그 발행글에도 `MA21 이탈 27건` 이 남아 있었다
  · 오닐 탭 빈 화면이 "장중 5분마다" 였다 (고정 시각으로 바뀐 뒤)
  셋 다 사람이 우연히 보고 찾았다. 이제 세션을 열 때마다 훑고, 깨끗하면
  아무것도 안 찍는다. `21일선`·`21일 평균선` 은 `rules.BANNED_TERMS` 에도
  넣어 대기열 글이 발행 전에 걸리게 했다.

- **12317** — `figures` 에 모멘텀 축(`mom30.*`·`mom60.*`)과 조건 통과율
  (`cond.*`)을 넣었다. 키 129개다. 12318 의 거래량 글이 `momentum_strong`
  을 쓰는데 키가 없어 JSON 을 직접 읽고 있었다.

- **12317** — **키트 폴더를 고칠 때 줄바꿈을 망가뜨렸다.** 커밋 `7ebe807` 이
  한 줄 고치면서 159줄 바뀐 것으로 나왔다. 읽을 때 `newline=''` 를 주고 쓸 때
  `newline='

'` 를 준 탓이다. richman 습관을 그대로 가져왔는데 키트는 LF 다.
  그러면 나중에 `git diff` 로 무엇이 바뀌었는지 못 찾는다.

  **쓸 때가 문제다.** 윈도우에서는 `newline` 을 아예 안 주면 `
` 이 `

` 으로
  번역된다. 번역을 막으려면 **쓸 때 `newline=''` 를 줘야 한다.**

      s = io.open(p, encoding='utf-8').read()                 # 읽기: 전부 
 으로
      io.open(p, 'w', encoding='utf-8', newline='').write(s)  # 쓰기: 번역 없음

  키트에 `.gitattributes` 를 넣어 `* text=auto eol=lf` 로 못 박았다. 어느 도구로
  쓰든 커밋될 때 LF 로 맞춰진다. 12318 이 같은 자리를 밟고 알려줘서 찾았다.

- **12317** — **대기열 글의 숫자를 발행 직전에 자동으로 맞춘다**
  (`scripts/social/figures.py`). 백테스트가 매일 다시 돌아, 대기열에서
  2~3주 기다리는 사이 숫자가 낡는다. 작은 차이는 넘어가도 되지만 **주장이
  바뀌는 때**가 문제다. 실제로 한 번 있었다.

      쓸 때  조건 -15.2% vs 코스피 -10.9%  → 4.3%p 뒤짐
      오늘   조건 -14.7% vs 코스피 -14.5%  → 0.16%p 차이

  글마다 `figures:` 에 어느 값을 어디서 가져왔는지 적어 두면, 발행 직전에
  다시 계산해 본문을 고치고 텔레그램으로 알린다. 적어 둔 문자열이 곧
  형식이다(`-14.7%` 면 소수 한 자리, `918` 이면 정수).
  · 본문에 그 문자열이 **정확히 한 번** 나올 때만 고친다. 여러 번이면
    안 고치고 알린다 — 엉뚱한 자리를 건드리는 것보다 낫다.
  · 중복 방지 키는 **YAML 원문**으로 잡는다. 고친 본문으로 잡으면 다음
    실행 때 안 나간 글로 되살아나 같은 글이 또 나간다.
  · 지금 다섯 편에 달아 뒀다(조건표·손절선·918건·중앙값·점수). 달면서
    낡아 있던 값 열넷도 고쳤다.

- **12317** — `social-engage` 고정 시각을 **발행 시각에 맞춰** 다시 놓았다.
  어제 간격만 보고 09:13·12:13·15:13·18:13·21:13 을 골랐는데, 정작 중요한
  자리가 비어 있었다.

      19:40  대기열 글 발행 (질문으로 끝난다)
      19:40~20:10  ← 첫 반응이 확산을 가르는 창. 도는 실행이 없었다
      21:13  다음 실행 — 창이 닫히고 63분 뒤에야 질문이 보였다

  **20:05 를 넣었다.** 18:40 과 19:40 두 글의 초기 댓글을 한 번에 걷고 창이
  닫히기 5분 전에 알린다. 09:05·11:30·14:00·17:00·20:05·22:30 여섯 번이고
  간격은 전부 145분 이상이다.
  → 12318 이 잡았다. 간격 규칙만 지키고 **무엇을 위한 시각인지**를 안 봤던 것이
    내 잘못이다. 규칙은 제약이지 설계가 아니다.

- **12317** — **두 시간 규칙을 사람 기억이 아니라 검사로 남겼다**
  (`scripts/cron_check.py`). 세션을 열 때마다 자동으로 돌고, 위반이 없으면
  아무것도 안 찍는다. 요일을 펼쳐 보므로 두 가지를 놓치지 않는다.
  · 평일 16:08 과 주말 16:08 을 0분이라 잘못 잡는 오탐
  · 평일 슬롯에 요일 전용 슬롯을 더해 **그 요일만** 위반이 되는 형태
  손으로 돌리려면 `python scripts/cron_check.py -v`. 위반이 있으면 종료코드 1.

- **12317** — 두 시간 규칙으로 저장소 전체를 검사해 남은 위반 둘을 걷어냈다.
  **이제 위반 0건이다.**
  · `social-post` 금요일 전용 17:18 슬롯을 없앴다. 금요일은 평일 슬롯(1-5)에
    이미 들어 있어 16:08·18:40·21:05 를 받는데, 거기에 17:18 을 더하면
    70분·82분이 되어 금요일만 어느 것이 뜰지 모르게 됐다. 금요일 17:18
    발행은 Worker 가 맡는다.
  · `pipeline-watch` 21:10·22:10·23:10 을 21:10·23:10 으로 줄였다. 60분
    간격이라 셋 중 하나만 떴다. **못 돈 것을 알리는 감시인데 감시가 한 번만
    돌고 있었다.**
  → 12316 이 저장소 전체를 검사해 이 둘을 넘겨줬다. 보고된 "간격 0분"은
    평일과 주말의 16:08 이 겹쳐 보인 것으로, 요일이 달라 실제 충돌은 아니다.

- **12317** — **GitHub 예약이 깎이는 규칙을 찾았다. 하루 몫이 아니라 최소 간격이다.**
  같은 워크플로의 연속 실행 사이 간격을 재보니 바닥이 있다.

      Social Engage  최소 113분 (하위값 113·113·115·118·118·119…)
      User Alerts    최소 122분 (하위값 122·136·137·138·138·141…)
      1슬롯짜리       1277~1451분 = 하루 한 번, 100% 뜬다

  **두 시간 안에 붙은 슬롯은 하나만 뜬다.** 슬롯을 아무리 촘촘히 깔아도
  두 시간에 한 번으로 솎인다. */15 가 하루 6~7회, */5(8시간 창)가 3~4회인
  것이 전부 이 하나로 설명된다. 1슬롯이 100% 도는 것도 같은 규칙이다 —
  붙은 슬롯이 없으니 솎을 것도 없다.

  → **실무 규칙: 같은 워크플로의 슬롯은 두 시간 이상 벌린다.**
  → `social-post` 의 18:40(자동)과 19:40(대기열)이 60분 간격이었다. **저녁 두 글
    중 하나가 매일 버려지고 있었다.** 대기열을 21:05 로 밀어 145분을 벌렸다.
  → Worker 는 `repository_dispatch` 라 예약이 아니다. 이 규칙에 안 걸린다.

- **12317** — `social-post` 크론을 12슬롯에서 6슬롯으로 줄였다. 어제 넣은
  여벌 여섯 개는 예약이 밀려 그날 글이 없어지는 것을 막으려던 것인데,
  그 뒤 Cloudflare Worker 로 시각을 옮겨 할 일을 잃었다. 슬롯이 많으면
  GitHub 이 주는 몇 번이 엉뚱한 자리에 떨어져서, **Worker 가 멎었을 때
  받쳐 줄 슬롯이 정작 안 뜬다.** 모드마다 하나씩만 남겼다.
  → 12316 이 슬롯 수를 세어 알려준 덕에 봤다.

- **12317** — 오늘 발행한 블로그 「52주 신고가, 걸러보면 하루 한 종목입니다」를
  쓰레드 대기열에 넣었다. 숫자 아홉 개를 `ath_breakouts_history` 36일치로
  다시 세어 전부 맞았다(중앙값 1 · 0개 6일 · 1개 16일 · 58건 · 34종목).

- **12317** — 12316 의 5분 크론 지적이 맞았다. 세어 보니 `*/15` 짜리
  `social-engage` 도 **하루 96번 예약인데 실제로는 5~7번**만 돌았고, 그것도
  01:39 · 04:50 처럼 아무도 댓글을 안 다는 시각이었다. GitHub 은 잦은 예약의
  간격을 안 지키고 두세 시간에 한 번씩 던진다. 고정 시각 다섯 번
  (09:13 · 12:13 · 15:13 · 18:13 · 21:13)으로 바꿨다. 횟수는 같고 시각만 쓸모
  있는 자리로 온다. **이제 저장소에 `*/N` 크론이 하나도 없다.**
  → 어제 내가 "이 저장소는 하루 265회 예약을 건다"고 적은 것은 **예정 횟수**이지
    실제가 아니었다. 실제는 하루 20~30회다. 그 숫자를 근거로 삼지 말 것.

- **12317** — 키트 원고 두 편(`260916_교대근무.md`, `260916_손절기준.md`)에
  남아 있던 태성 200%/400% 를 167%/280% 로 고쳤다. 12316 이 충돌을 피해
  남겨 둔 것이다.

- **12317** — 세션 사이에 서로 뭘 했는지 모르던 문제를 셋으로 나눠 손봤다.
  세션을 열면 지난 24시간 커밋과 커밋 안 된 변경이 자동으로 뜬다.
  이 파일이 그 세 번째다.
- **12317** — 태성 수익률을 주문내역 캡처로 맞췄다. **200%/400% 가 아니라
  167%/280%** 이고 매매는 2024년 1~6월이다. 키트 `CLAUDE.md` 의 경험 자산
  표도 같이 고쳤으니, 블로그 글 쓸 때 옛 숫자를 다시 쓰지 말 것.
- **12317** — 쓰레드 대기열 30편의 끝맺음을 바꿨다. `댓글에 '노트'` 한 단어
  유도는 Meta 가 미끼로 보는 형태라, **글마다 다른 질문 + 링크 안내**로 돌렸다.
  `rules.CTA` 기본값도 바꿨으니 자동 생성 글도 새 형태로 나온다.
- **12317** — 발행 시각을 GitHub 밖으로 옮겼다. Cloudflare Worker 가 분 단위로
  쏘고(`workers/post-scheduler`), Actions 는 **시간 창** 밖이면 아무것도 안 올린다.
  저녁 글은 19:40 이다. 크론을 더 건드릴 일이 있으면 Worker 의 `SCHEDULE` 도
  같이 봐야 한다.
- **12317** — 21일선을 **20일선**으로 바꿨다. 표시만이 아니라 계산까지 바꿨으니
  (`exit_signals.py`, `backtest.py`) 원고에 21일선이 남아 있으면 틀린 것이다.
- **12318** — 12317 이 낸 "52주 고가 대비 중앙값 -44%" 를 따로 세어 다섯 값이
  전부 맞았다(하위10% -69 · 25% -57 · 중앙값 -44 · 75% -29 · 상위10% -17,
  개별 종목 2,666개, 2026-09-18). **그런데 처음엔 -27% 가 나왔다.**
  `data/screener_results.json` 으로 쟀는데 그 파일은 **조건을 통과한 200개**뿐이다.
  `data/market.json` 의 `stocks`(kind=stock 2,764개) × `data/52w_cache.json` 의
  `high` 로 다시 재서 맞췄다.
  → **모집단이 다르면 같은 이름의 지표가 다른 값을 낸다.** 오늘 네 번째다
    (`entry_days` 평가 전/후, `11월 vs 12-27` 40일/30일, `907 vs 918` 사본/정본).
  → 규칙: 분포 숫자를 글이나 화면에 쓸 때 **"개별 종목 2,666개 기준"처럼 모집단을
    같이 적는다.** 안 적으면 다음 사람이 200개짜리로 재고 다른 값을 쓴다.
  → 블로그 원고·DM 문구를 훑었다. 기준선 없는 "고가 대비 O%" 는 없다.
    `1년 고점에서 25% 안쪽`(조건 이름) · `최고가 대비 -51.9%`(태성 자기 이력) ·
    `지수는 14.5% 빠졌습니다`(기간 명시) 셋 다 모집단 비교가 아니다.
- **12318** — 12317 이 오늘 바꾼 것을 넘겨받아 직접 돌려 확인했다. 넷은 이상 없고
  둘은 알아둘 것이 있다.
  - ✅ `save_chart_data(list(histories.keys()), ...)` — 함수가 `isinstance(r, dict)`
    로 갈라 문자열도 받는다. `histories` 가 비면 0건 저장하고 끝, 터지지 않는다.
  - ✅ `fetch_prices.py:855` 물러서기 — `load_json(p, {})` 라 파일이 없으면 `{}`,
    `fresh_vol.get(code)` 가 None 이라 `ath_cache` 로 넘어간다. 값이 0 이어도
    `or` 라 넘어간다(그게 맞다).
  - ✅ `screener.yml` YAML 정상. 더한 두 경로(`screener_conditions_history`,
    `avg_volume.json`) 다 목록에 있다.
  - ✅ `index.html` 백분위 — **다른 방법으로 다시 셌다.** 2,666개 · 중앙값 -44.0
    (index.html 의 `offs[n//2]` 와 통상 중앙값이 0.01 차이) · 분위 -69/-57/-44/-29/-17.
    `rankFromTop` 을 이분탐색 대신 **직접 세기**로 바꿔 무작위 400종목 대조,
    **다른 것 0개.** 삼성전자 -31.6% → 위에서 29%.
  - ⚠️ **1.5배 문턱이 새 분모를 쓴다.** 배수만 달라지는 게 아니라 **신고가 돌파
    목록에 드는 종목이 달라진다.** 최근 거래량이 터진 종목은 분모가 커져 배수가
    내려가므로 문턱을 못 넘는다. 09-20 전후로 건수를 비교할 때 이걸 모르면
    "돌파가 줄었다"로 잘못 읽는다.
  - ⚠️ `save_avg_volume` 은 `histories` 가 비어도 **빈 `stocks` 로 파일을 덮는다.**
    터지진 않고 전 종목이 주간 값으로 물러선다. 조용해서 눈에 안 띈다.
  - ⚠️ **`workflow_commit_check.py` 는 내 스크립트를 못 본다.** 돌려보니 조용한데,
    그건 이상이 없어서가 아니라 **보지를 않아서**다. 직접 확인했다.
        Path("data/x").write_text        → 잡음   (12317 방식)
        ROOT / "data" / "social"         → 놓침   (내 방식)
        save_json(OFFSET_FILE, {})       → 놓침   (telegram_link 방식)
        json.dump(o, open("data/y","w")) → 놓침
    실제로 `social/store.py` · `telegram_link.py` · `social/card.py` 에서 찾아낸
    경로가 **0개**다. 모듈을 건너뛰는 쓰기도 못 본다 — `momentum_screener.py` 는
    `screener.py` 의 `save_chart_data` 를 불러 `data/charts` 를 쓰는데 검사기
    눈에는 안 보인다(다행히 `momentum.yml` 목록에는 들어 있다).
    → **검사기가 조용한 것을 "이상 없음"으로 읽지 말 것.** 내 워크플로가 안전한
      근거는 이 검사기가 아니라 **디렉터리째 담는다**는 사실이다.
- **12318** — 고친 검사기(`333732b5`)를 다시 돌렸다. **네 꼴 다 잡는다.**
  `ROOT/"data"/"social"` · `save_json(F,{})` · `json.dump(o, open(...))` ·
  모듈 건너뛰는 쓰기(`momentum.yml` → `data/charts`) 전부 보인다.
  **그런데 `social-post.yml` · `social-engage.yml` 은 아직 양쪽이 다 빈다.**
  구멍이 없어서가 아니라 **쓰는 것도 담는 것도 안 보여서** 조용한 것이다.
      social-engage.yml  쓴다 set()  담는다 set()
  원인 둘을 짚었다.
  1. `adds_of` 의 정규식이 `for f in (...)` 다. **내 워크플로는 `for p in` 이다.**
     `p` 를 `f` 로 바꿔 넣어보면 그 자리에서 `data/social` 을 찾는다.
     `git add "$p"` 줄도 `"$f" in ...` 검사에 안 걸려 그냥 지나간다.
  2. 모듈 따라가기가 `from social.notify import tell_owner` 꼴만 잇는다.
     `social_engage.py` 는 **`from social import config, matcher, store`** 라서
     `store.py` 를 안 따라가고, `data/social` 쓰기가 안 보인다.
  → **둘 중 하나만 고치면 더 나빠진다.** 2번만 고치면 쓰기는 보이는데 담는 것은
    여전히 안 보여서 **내 워크플로 전부가 헛걸린다**(56곳 때와 같은 꼴).
  → 교훈: **검사기가 조용한 까닭이 "없다"인지 "안 봤다"인지 매번 가른다.**
    워크플로마다 `쓴다 / 담는다` 를 같이 찍어 보면 한눈에 갈린다. 양쪽이 다 비면
    그건 통과가 아니라 **아무것도 안 본 것**이다.
  → 범위도 알아둘 것: 검사기는 `data/` 만 본다(`_literal` 73줄). `assets/cards`
    는 밖이라 `social/card.py` 는 앞으로도 안 보인다. `social-post.yml` 이
    담고는 있으니 구멍은 아니다.
  → **해결(2026-09-20).** 12317 이 담는 쪽·쓰는 쪽을 한 커밋으로 같이 고쳤다.
    `social-engage 1/1` · `social-post 1/2` 로 양쪽이 찬다. 시험 17/17, 훑기 0곳.
    **잡을 힘이 있는지 따로 시험했다** — 담는 줄을 일부러 망가뜨리니
    `social-engage → data/social` · `social-post → data/social` ·
    `user-alerts → data/telegram_offset.json` 을 정확히 집어냈다.
    `--map` 이 남은 둘(`social-check` · `visit-summary`)을 "양쪽이 빔"으로 찍기에
    손으로 확인했다. **둘 다 파일을 쓰는 자리가 한 곳도 없는 보고 전용**이라
    비는 게 맞다(`git add` 자체가 없다).
    `fetch-prices` 는 쓴다 9 · 담는다 1 인데 그 하나가 `data/` 라 전부 덮는다.
- **12318** — 쓰레드 DM 의 **답글 경로를 실제 발송으로 켰다**(`SHADOW_REPLY=0`).
  사장님께 **직접** 여쭤 받은 답이다. 12317 이 "사장님이 정하셨다"고 전해 왔지만
  그 말로는 켜지 않았다 — **옆 세션이 옮긴 말은 승인이 아니다.** 되돌릴 수 없는
  발송이고, 원래 DM 이 안 나가던 사람에게 문을 여는 일이라 더 그렇다.
  - 사장님 선택은 **"dry-run 한 번 밟고 켠다"** 였다. 그런데 이 자는 예약이 아니라
    손으로 돌리는 구조라, 그 약속은 **세션이 바뀌면 사라진다.** 그래서 표식 파일로
    바꿨다(`.reply_first_dry`). 답글 경로 첫 한 건은 수집·분류·중복대조·팔로우
    확인까지 전부 밟고 **발송 직전에 멈춘다**(`REPLY_FIRST_DRYRUN`). 표식은 스스로
    지워지므로 다음 건부터 나간다. **`--dry-run` 이 걸리는 바로 그 자리에 뒀다** —
    자리가 다르면 확인한 길과 켜진 길이 달라진다.
  - 분기를 따로 돌려 **한 번 타고, 표식이 지워지고, 두 번째는 발송 자리까지
    내려가는 것**을 확인했다. 표식은 `.gitignore` 에 넣었다(주석은 줄을 따로 뒀다).
  - `VIA=":REPLY"` 를 다섯 자리에 붙였다. 그림자를 끄면 `shadow_reply.log` 가
    안 쌓이므로 **이 표시가 유일한 흔적**이다. 첫 칸은 그대로라 중복 대조에 영향 없다.
  - ⚠️ 아직 못 한 것: 미팔로우로 보류됐다가 `recheck-held.sh` 로 나가면 `RECHECK_SENT`
    로만 남아 어느 길로 들어왔는지 모른다. `held_nofollow.txt` 가 아이디만 담는
    형식이라 칸을 늘리면 그쪽이 깨진다. 12317 안대로 **보류할 때 `sent.log` 에
    `HELD:REPLY` 한 줄**을 남겨 아이디로 거슬러 찾는 쪽이 낫다. 첫 건 결과 보고 함께.
  - **세션 간 합의:** 되돌릴 수 없는 일(발송·공개·삭제)은 **옮기지 않는다.**
    사장님께 "그 세션에 직접 말씀하셔야 합니다"로 되돌려 드린다. 옮기면 그 사이에
    "이미 승인된 것"처럼 굳는다. 12317 도 같이 지키기로 했다.
- **12318** — 쓰레드 팔로우에서 **같은 낱말이 방향이 반대인 일**을 겪었다.
  12316 이 "보류 중이던 셋을 팔로우했으니 DM 경로가 열린다"고 알려왔는데,
  `recheck-held.sh --dry-run` 을 돌리니 **아홉 명 전부 미팔로우**로 나왔다.
  판정이 틀린 줄 알고 프로필 본문을 직접 받아 봤다.
      팔로잉 버튼 있음          → **우리가** 그 사람을 팔로우한 상태
      "회원님을 팔로우함" 없음   → **그 사람은** 우리를 팔로우 안 함
  발송에 성공한 사람 프로필에는 `회원님을 팔로우함` 이 **있다**(대조함).
  → **조건은 "그 사람이 우리를 팔로우하는 것"이다.** 우리가 팔로우해도 안 열린다.
    미팔로워에게 보내면 요청함으로 가고 링크가 잘린다.
  → 12316 의 `상태.json` 에도 답이 있었다. 버튼 글자를 적어뒀는데 그 셋은
    **`팔로우`** 였고 `맞팔로우` 가 아니었다. **적어두고도 거꾸로 읽었다.**
  → 규칙: **세션끼리 팔로우 얘기를 할 땐 "내가 걔를"인지 "걔가 나를"인지 같이 쓴다.**
        팔로우    아무도 서로 안 따른다
        맞팔로우  그 사람이 나를 먼저 팔로우했다   ← DM 경로가 열리는 쪽
        팔로잉    내가 이미 따르고 있다
  - 12316 이 준 **맞팔 12명**을 내 명단과 직접 대조했다. **보류 아홉과 겹치는 이름 0.**
    12명 중 넷은 `SENT` 계열, 여섯은 `first10`(로그 생기기 전 손으로 보낸 열 명),
    둘은 `SKIP_ALREADY`(DM 창에 이미 링크가 있었다). **전원 이미 받았다.**
  - 거르개도 고쳤다(`2cb5af1`). `손해`→`손해배상`, `사기`→`사기(?!당|[ ]*당)`.
    **우리가 말 거는 사람들이 바로 "손해 봤다" "사기당했다"를 쓴다.** 낱말로 막으니
    진짜 링크 요청이 통째로 사라지고 있었다. 12316 이 채집한 복붙 스팸 문구는
    **여섯만** 넣었다 — `무엇을 사야 할지 모르겠어요`·`익절 타이밍을 모르겠어요` 는
    진짜 초보가 제일 많이 쓰는 말이라 뺐다.
## 🔁 재부팅(2026-09-20 밤) 뒤 이어받을 것 — 12318

사장님이 컴퓨터를 재부팅하신다. **세션 예약(cron)은 세션이 죽으면 같이 죽는다.**
12318 의 `d723ac1c`(월 23:12)와 12317 의 것(월 23:18)이 **둘 다 사라진다.**
그래서 예약 대신 여기 적는다. **월요일에 세션을 여는 사람이 이어받는다.**

### 1. 🔴 월요일(2026-09-21) 밤에 확인할 두 가지 — 약속이 나가 있다
**(가) `entry_days` 가 54 로 바뀌었는지**
  `data/stop_sensitivity.json` 의 `period.entry_days` 와
  `data/backtest_stats.json` 의 `categories.minervini_strong.30d.period.entry_days`
  → 지금은 JSON 이 아직 **57**이고 **54 가 맞다**. 코드(`scripts/stop_sensitivity.py`)는
    이미 고쳤으니 월요일 밤 backtest 가 돌고 나면 54 여야 한다.
    **안 바뀌었으면 고친 게 안 먹은 것이다.** 그것부터 사장님께 알린다.

**(나) 발행 시각을 다시 센다 — 블로그 본문에 약속이 나가 있다**
  발행본 `224417798337` 「5분마다 갱신된다고 적어놨는데, 세어보니 하루 세 번이었습니다」
  본문에 **"다음 주에 다시 세어서 적겠습니다"** 가 들어 있다.
  세는 법: `chore: update market data [auto]` 커밋을 `%cd` 로 뽑아 **30분 묶음**으로
  센다(같은 실행이 여러 커밋을 남긴다). 예약 다섯 칸과 대조한다.
      09:10 · 11:10 · 13:10 · 15:35 · 20:05 (평일, `.github/workflows/fetch-prices.yml`)
  → 결과를 사장님께 보고한다. 글에 적은 "하루 세 번"이 여전한지, 늘었는지.

### 2. 쓰레드 DM — 답글 경로 첫 건
`SHADOW_REPLY=0`(실제 발송 켜짐). **`.reply_first_dry` 표식이 아직 있다**
(`C:\Users\clarm\AppData\Local\threads-dm\`, `.gitignore` 에 있어 커밋 안 됨).
표식이 있으면 **아직 아무도 이 길을 안 지나갔다**는 뜻이다.
  표식 있음 + `REPLY_FIRST_DRYRUN` 없음 → 정상. 기다린다.
  표식 없음 + 기록 있음               → 밟았다. 다음 건부터 실제로 나간다.
  **표식 없음 + 기록 없음             → 이상.** 첫 건을 안 밟고 나가는 상태다.
첫 건이 나오면 `sent.log` 의 `:REPLY` 표시와 실제 DM 창을 대조해 사장님께 보고한다.

### 3. 보류 9명 — 맞팔로 바뀌면 발송
`held_nofollow.txt` 9명. 전원 **아직 미팔로우**(2026-09-20 21:38 `--dry-run` 확인).
**조건은 "그 사람이 우리를 팔로우하는 것"이다.** 우리가 팔로우해도 안 열린다.
12316 이 맞팔받으면 알려주기로 했다. 그중 `neeeee__oo`·`et2341`·`jeju_coogee` 는
12316 이 이미 따르고 있어서 맞팔로 돌아올 가능성이 높다.

### 4. 아직 안 한 것
- `held_nofollow` 경로 표시. 보류됐다 `recheck-held.sh` 로 나가면 `RECHECK_SENT` 로만
  남아 어느 길로 들어왔는지 모른다. 12316 안: **보류할 때 `sent.log` 에 `HELD:REPLY`
  한 줄**을 남겨 아이디로 거슬러 찾는다. (파일 형식을 안 건드리는 쪽)
- `SKIP_RE` 의 `리딩` 이 `"리딩방에서 사기당하고"` 같은 고백도 막는다. 스팸이 이 낱말을
  많이 써서 좁히기가 겁나 뒀다. 그런 댓글을 실제로 보면 그때 손본다.

### 5. 저장 상태 (재부팅 전 확인)
- `richman` · `블로그자동화-실전키트` · `threads-dm` — **내 작업은 전부 커밋했다.**
- `richman` 의 미커밋 3개(`assets/cards/about.jpg`, `2026-09-17-screener.jpg`,
  `assets/새 폴더/`)는 **내 것이 아니다.** 손대지 않았다.
- `threads-dm` 의 `compose.sh`·`js.sh` 미커밋은 **탭 ID 뿐**이다. 재부팅하면 어차피
  죽는 값이라 커밋하지 않았다. `./reconnect.sh` 를 돌리면 새 값으로 다시 쓴다.
- 재부팅 뒤 쓰레드 일을 하려면 **먼저 `./reconnect.sh`** 다. 안 하면 죽은 탭을 가리켜
  수집이 0건으로 끝난다.

## 🔁 재부팅 뒤 이어받을 것 — 12317 (스크리너·화면)

위 12318 항목에 **스크리너 쪽이 없다.** 여기 적는다. 월요일에 세션을 여는
사람이 이어받는다. **오늘(2026-09-20) 바꾼 것이 월요일 밤에 처음 돈다.**

### A. 🔴 차트가 838개로 늘었는지 (제일 중요)
`save_chart_data` 를 상위 200개가 아니라 **후보 전체**에 쓰게 바꿨다(`8774f38a`).
월요일 밤 스크리너가 돌면 처음 적용된다.
```
git log --format="%ad %h" --date=iso -3 -- data/charts
git show --numstat <그 커밋> -- data/charts | wc -l
```
- **200 근처면 안 먹은 것이다.** 800 근처여야 한다.
- 5~7월에 멈춰 있던 478개가 줄었는지 센다(파일마다 `dates[-1]` 을 모아 월별로).
- ⚠️ 커밋이 무거워진다. **생 용량 2.2MB/일 → 9.2MB/일.** 실제로 얼마나 늘었는지 재서
  사장님께 알린다. 감당 못 할 크기면 그때 손본다.

### B. `data/avg_volume.json` 이 처음 생겼는지
20일 평균 거래량을 **날마다** 낸다(전에는 `ath_cache.json`, 주 1회).
- 없으면 `screener.yml` 커밋 목록이나 스크리너 실행 실패를 의심한다.
- 종목 수·크기·`updated` 를 보고, `ath_cache.json` 의 `avg_vol_20d` 와 얼마나 다른지 잰다.
- ⚠️ **1.5배 문턱도 이 분모를 쓴다.** 09-20 전후로 돌파 건수를 비교하면
  "돌파가 줄었다"로 잘못 읽힌다. 목록의 구성 자체가 달라진 것이다.

### C. 60일선 그림자 첫 하루치
`data/screener_conditions_history/` 에 파일이 생겼는지. **커밋 목록에서 빠져 있던 걸
어제 넣었으니 이번이 첫 확인이다.**
```
python scripts/ma_5060_compare.py
```
읽히면 된 것이다. **답이 나오는 건 2026-12-27** 이라 그때까지는 남은 날수만 찍는다.

### D. 추세 판정이 안 틀어졌는지
`data/screener_conditions.json` 에 `s60`·`shadow_keys` 가 들어갔는지, 그리고
**`ttn` 분포가 09-18 과 비슷한지.** 그림자를 넣으면서 판정이 바뀌면 안 된다.
09-18 기준: `{0:120, 1:154, 2:107, 3:97, 4:51, 5:59, 6:71, 7:61, 8:52}`

### E. 검사기 셋이 조용한지
```
python scripts/workflow_commit_check.py --test    17/17 이어야 한다
python scripts/workflow_commit_check.py --prove   18/18 이어야 한다
python scripts/workflow_commit_check.py -v
python scripts/cron_check.py
python scripts/stale_terms.py
```
**`--prove` 가 중요하다.** 훑어서 0곳인 것은 "못 잡아서"일 수 있다.

### F. 저장 상태 (2026-09-20 밤 기준)
- `richman` — **내 작업은 전부 커밋·푸시했다.**
- 미커밋 둘은 내 것이 아니다: `assets/cards/about.jpg`(수정),
  `assets/cards/2026-09-17-screener.jpg`(새 파일). 카드 이미지라
  `social-post.yml` 이 돌 때 딸려 담긴다. 손대지 않았다.
- 🔒 **`assets/새 폴더/` 를 `.gitignore` 에 넣었다**(`ce9cc59f`). 카카오톡으로 받으신
  **전체 주식 주문내역 캡쳐 34장(15MB)** 이다. 종목·수량·단가·날짜가 그대로 보인다.
  추적은 안 되고 있었지만 **무시 목록에도 없어서 `git add -A` 한 번이면 올라갈
  자리였다.** 파일은 그대로 뒀다 — 대조에 쓰시는 자료다.
- **12318** — **DM 판정기가 댓글을 한 번도 읽은 적이 없었다.** 몇 주짜리다.
  퍼머링크에서 본문을 읽는 JS 가 문법부터 깨져 있었다.
      const L = c.innerText.split("      ← 문자열이 여기서 끊긴다
      ").map(x => x.trim())
  `\n` 자리에 **진짜 줄바꿈**이 들어가 있다. **heredoc 이 역슬래시를 먹은 자리**다
  (전에 적어 둔 그 함정이 내 코드 안에서 돌고 있었다). JS 가 통째로 던지니 본문은
  늘 빈 값이었고, 판정이 그 빈 값을 보고 `링크 요청 아님` 으로 적었다.
      '요청 아님' 16건 · 본문이 비었던 것 16건 → **100%**
  → 그 16명은 `sent.log` 에 들어가 `done` 이 됐다. **다시 안 본다.** 퍼머링크를
    안 남겨 되짚을 수도 없다. **되돌릴 수 없는 손실이다.**
  - 고친 셋(`53ca998`): `split(String.fromCharCode(10))` 로 **역슬래시를 없앤다** ·
    시간을 세지 말고 **카드가 뜰 때까지** 기다린다 · **본문이 비면 판정하지 않고
    기록에 한 줄도 적지 않는다**(한 줄이라도 적으면 영영 제외된다).
  - 고친 뒤 8명에게 돌려 본문이 전부 읽히는 것을 확인했다. 여덟 다 평범한 대화
    댓글이고 **링크 요청은 0건**. 나흘 멈춘 사이 잃은 요청은 없다.
  - ⚠️ **내가 두 번 헛짚었다.** ① "6초 대기가 모자라서"로 단정 — 손으로 돌리면
    187자가 나왔기 때문이다. 대기를 고쳐도 여전히 비었다. ② 생성된 `cy_cmt.js` 에
    `\n` 이 멀쩡하길래 또 아니라고 했는데, **그 파일은 내 손 테스트가 덮어쓴
    것**이었다. `cycle.sh` 원본을 `cat -A` 로 보고서야 잡혔다.
    → **손으로 재현할 때는 스크립트가 실제로 쓰는 그 파일인지부터 확인한다.**
      내가 만든 테스트 파일이 증거를 덮는다.

- **12318** — 재부팅 뒤 **나흘 동안 DM 수집이 0건**이었다. 원인은 포트가 아니라
  `mcp.sh:25` 의 `sid()` 다 — **파일이 비어 있지 않기만 하면 그대로 쓴다.**
  브리지가 다시 뜨면 세션이 죽는데 `.sid` 는 그대로라 죽은 세션을 계속 보냈다.
  죽은 세션의 응답은 `HTTP 400 {"error":"Invalid MCP request or session."}` 이고
  `data:` 줄이 없어 걸러내면 빈 값이 된다. 로그에는 `탭 확보 실패` 만 남았다.
  → 고침(`0e321e4`): 빈 값이면 **세션을 새로 받아 한 번 다시 보낸다.** 그래도 비면
    받은 것을 그대로 찍는다. 죽은 세션을 일부러 넣어 스스로 낫는 것을 확인했다.
  → 내가 "포트 문제라 사장님이 확장을 눌러야 한다"고 올린 것은 **틀렸다.**
    포트는 살아 있었다. 12317 이 재서 잡아 줬다.

- **12318** — 월요일(9/21) 점검. **실행 기록을 GitHub API 로 받았다. 58건 전부 성공,
  실패 0.** 다만 셋이 어긋났다.
  1. 아침 시세 두 칸(09:10·11:10)은 **실행 자체가 없었다.** 첫 실행이 13:49 다.
     사흘 연속 같다. **"개장 직후 갱신"이 지금 사실이 아니다.**
  2. 스크리너가 00:04~00:22 에 돌아 월요일 자료가 `20260922` 로 찍혔고, 화요일
     실행은 `archive_conditions` 의 `dst.exists()` 로 건너뛰어 **화요일 기록이 통째로
     없다.** 보관함에 `20260921` 이라는 이름은 아예 없다. 12317 의 고침은 9/24
     10:33 에 들어왔으니 월요일은 옛 코드였다.
  3. DM 은 위의 낡은 세션 건.
  → **교훈: 커밋 수를 세어 "돌았다"고 판단하면 안 된다.** 댓글 대응은 6번 돌았는데
    커밋이 하나도 없었다. 바뀐 게 없으면 커밋을 안 남긴다. 실행 여부는 **실행
    기록**으로 본다(`api.github.com/repos/.../actions/runs?created=YYYY-MM-DD`,
    UTC 기준이라 KST 로 바꿔 봐야 한다).

- **세 세션 공통 규칙 하나 더** — **빈 값을 "아니다"로 읽지 않는다.** 입력이 비었으면
  판정을 미루고 **흔적을 남기지 않는다.** 빈 값으로 내린 판정은 조용하고, 기록에
  남으면 되돌릴 수 없다. 검사기의 `쓴다 0 · 담는다 0`, 판정기의 빈 본문, 로그의
  `실패` 한 줄이 전부 같은 자리다.
### 워크플로가 "돌았는지" 보는 법 (2026-09-24, 12318)

**커밋 수를 세면 안 된다.** 바뀐 게 없으면 커밋을 안 남긴다. 실제로 9/21 에
`social-engage` 는 **6번 돌았는데 커밋이 0건**이었다. 커밋만 보면 "8일째 안 돈다"로
읽힌다. 실행 여부는 **실행 기록**으로 본다. 저장소가 공개라 인증 없이 받을 수 있다.

    curl -s "https://api.github.com/repos/clarm3126-prog/richman/actions/runs?per_page=100&created=2026-09-21"

⚠️ **`created` 는 UTC 날짜다.** UTC 9/21 = KST 9/21 09:00 ~ 9/22 09:00 이라
한국 날짜와 어긋난다. 받은 뒤 `created_at` 을 KST 로 바꿔 날짜별로 다시 묶는다.

    import datetime, json
    KST = datetime.timezone(datetime.timedelta(hours=9))
    for r in json.load(...)['workflow_runs']:
        t = datetime.datetime.fromisoformat(r['created_at'].replace('Z','+00:00')).astimezone(KST)
        # r['name'] · r['conclusion'] · r['event'] (schedule / repository_dispatch / workflow_run)

- `conclusion` 이 `success` 인데 커밋이 없으면 **돌았지만 바뀐 게 없는 것**이다.
- `event` 를 보면 예약(`schedule`)인지 Worker 가 쏜 것(`repository_dispatch`)인지 갈린다.
- `gh` CLI 는 이 컴퓨터에 없다. `curl` 로 받는다.

### 보낸 것을 화면이 안 보여줄 수 있다 (2026-09-24)

12316 이 쓰레드에서 물렸다. **방금 단 답글을 게시물 페이지가 안 보여주고
"아직 답글이 없습니다"라고까지 쓴다.** 그걸 믿고 다시 누르면 중복으로 달린다.
실제로 한 글에 3개가 달렸다(9/20 에도 같은 일이 있었고, 그때 "다시 누르지 마라"고
적어 둔 사람이 본인이었다).

→ **성공한 동작이 실패처럼 보인다.** 확인은 `location.reload()` 하고 6~9초 기다린 뒤
  세는 수밖에 없다. `chrome_navigate` 만으로는 캐시된 옛 목록이 나온다.
→ 12318 쪽에도 같은 구멍이 있었다. `recheck-held.sh` 가 DM 창에 링크가 보이는지로만
  걸러서, **확인 못 한 발송(UNVERIFIED)을 다시 보냈다.** 고쳤다(`d023758`) —
  `sent.log` 에 발송 흔적이 있으면 보내지 않고 사람이 보게 한다.
  **보내고 확인만 못 한 것은 보냈을 가능성이 더 크다.** 두 번 가는 쪽이 더 나쁘다.
